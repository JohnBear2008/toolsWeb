<!-- 导入css库 -->
<link rel="stylesheet" type="text/css" href="/rpLib/css/divCss.css">

<!-- 导入js库 -->
<script type="text/javascript" src="/rpLib/publicFuns.js"></script>
<script type="text/javascript" src="/rpLib/addonFuns.js"></script>

<!-- 导入datatables -->
<link rel="stylesheet" type="text/css" href="/rpLib/DataTables/datatables.min.css" />
<script type="text/javascript" src="/rpLib/DataTables/datatables.min.js"></script>

<!-- 导入bootstrapSelect -->
<link rel="stylesheet" href="/rpLib/bootstrapSelect/css/bootstrap-select.min.css" />
<script type="text/javascript" src="/rpLib/bootstrapSelect/js/bootstrap-select.min.js"></script>


<!-- 导入bootstrapSwitch -->
<link rel="stylesheet" href="/rpLib/bootstrapSwitch/css/bootstrap3/bootstrap-switch.min.css" />
<script type="text/javascript" src="/rpLib/bootstrapSwitch/js/bootstrap-switch.min.js"></script>

<!-- 导入datatimepicker -->
<link href="/rpLib/datetimepicker/bootstrap-datetimepicker.min.css" rel="stylesheet" media="screen">
<script type="text/javascript" src="/rpLib/datetimepicker/bootstrap-datetimepicker.min.js" charset="UTF-8"></script>
<script type="text/javascript" src="/rpLib/datetimepicker/bootstrap-datetimepicker.zh-CN.js" charset="UTF-8"></script>


<!-- 导入qrcode 二维码库 -->
<script type="text/javascript" src="/rpLib/qrcode/qrcode.min.js"></script>

<!-- 导入instascan 二维码扫描库 -->
<script type="text/javascript" src="/rpLib/instascan/instascan.min.js"></script>

<!-- 导入print 打印库 -->
<script type="text/javascript" src="/rpLib/print/print.js"></script>

<!-- 导入js-xlsx -->
<script type="text/javascript" src="/rpLib/sheetJs/xlsx.core.min.js"></script>



<!-- 定义html元素 -->
<div class="rpContent">

    <!-- 按钮：用于打开模态框 -->

    <!-- <div class="row clearfix">
        <div class="col-md-12 column">
            <div class="page-header">
                <h1>库存管理-调拨单</h1>
            </div>
        </div>
    </div> -->


    <div class="row clearfix">
        <div class="col-md-12 column">


            <!-- 定义基础模块数据表 -->

            <div class="panel panel-default" id='mainTablePanel'>
                <div class="panel-heading">
                    <div style="float: left;">
                        <div class="btn-group">
                            <button type="button" class="btn btn-primary">过滤</button>
                            <button type="button" id="btnFilterToday" class="btn btn-default">当天</button>
                            <button type="button" id="btnFilterUnAudit" class="btn btn-default">待审核</button>
                            <button type="button" id="btnFilterDiy" class="btn btn-default">自定义</button>
                        </div>
                    </div>
                    <!-- excel -->
                    <div style="float: left; margin-left: 15px;">
                        <div class="btn-group">
                            <button type="button" class="btn btn-primary">excel</button>
                            <button type="button" id="batchImportBtn" class="btn btn-default">批量导入</button>
                            <button type="button" id="batchExportBtn" class="btn btn-default">批量导出</button>
                            <button type="button" id="importModelBtn" class="btn btn-default">数据模板</button>
                            <input type="file" id="file" style="display:none;"
                                accept="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet">
                        </div>
                    </div>
                    <!-- 功能组按钮 -->
                    <div style="float:right">

                        <button id='btnTransferBillNew' type="button" class="btn btn-info">新增</button>
                        <button id='btnTransferBillAudit' type="button" class="btn btn-warning">审核</button>
                        <button id='btnTransferBillDelete' type="button" class="btn btn-danger">删除</button>

                        <!-- <button type="button" class="btn btn-info" id="listPrintBtn">
                            打印调拨码
                        </button> -->
                        <button type="button" class="btn btn-primary" id="rp_transferbillsPrint">
                            打印调拨单
                        </button>
                    </div>
                    <div style="clear:both"></div>
                </div>
                <div class="panel-body">
                    <table id="rp_transferbills" class=" table table-striped table-bordered" style="width:100%">
                        <thead>
                            <tr>
                                <th>调拨单号</th>
                                <th>拨出仓库</th>
                                <th>拨入仓库</th>
                                <th>制单人</th>
                                <th>制单日期</th>
                                <th>审核人</th>
                                <th>审核日期</th>
                                <th>备注</th>
                                <th>单据状态</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>

            <!-- 定义主表关联附表数据表 -->

            <div class="panel panel-default" id='subTablePanel' style="width:100%;display: none;">
                <div class="panel-heading">
                    <h3 class="panel-title">产品清单</h3>
                </div>
                <div class="panel-body">
                    <table id="rp_transferbillsSub" class=" table table-striped table-bordered" style="min-width:100%;">
                        <thead>
                            <tr>
                                <th>序号</th>
                                <th>产品编号</th>
                                <th>产品描述</th>
                                <th>单位</th>
                                <th>数量</th>
                                <th>备注</th>
                                <th>状态</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>


        </div>
    </div>

    <!-- 过滤器模态框（Modal） -->
    <div class="modal fade" id="filterModal" tabindex="-1" role="dialog" aria-hidden="true">

        <div class="modal-dialog" style="width: 800px;">

            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
                        &times;
                    </button>
                    <h4 class="modal-title">
                        过滤器
                    </h4>
                </div>
                <div class="modal-body">

                    <div class="panel panel-info">
                        <div class="panel-body" style="height:300px">
                            <form class="form-horizontal" role="form" id='filterForm'>

                                <!-- <div class="form-group">
                                    <label class="col-sm-2 control-label">调拨日期</label>
                                    <div class="col-sm-5">
                                        <div class="input-group date form_date ">
                                            <input id="filterForm-borrowDateB" class="form-control " size="16"
                                                type="text" value="" placeholder="起始日期" readonly>
                                            <span class="input-group-addon"><span
                                                    class="glyphicon glyphicon-calendar"></span></span>
                                        </div>
                                    </div>

                                    <div class="col-sm-5">
                                        <div class="input-group date form_date ">
                                            <input id="filterForm-borrowDateE" class="form-control " size="16"
                                                type="text" value="" placeholder="结束日期" readonly>
                                            <span class="input-group-addon"><span
                                                    class="glyphicon glyphicon-calendar"></span></span>
                                        </div>
                                    </div>

                                </div> -->

                                <div class="form-group">
                                    <label class="col-sm-2 control-label">制单日期</label>
                                    <div class="col-sm-5">
                                        <div class="input-group date form_date ">
                                            <input id="filterForm-makeDateB" class="form-control " size="16" type="text"
                                                value="" placeholder="起始日期" readonly>
                                            <span class="input-group-addon"><span
                                                    class="glyphicon glyphicon-calendar"></span></span>
                                        </div>
                                    </div>

                                    <div class="col-sm-5">
                                        <div class="input-group date form_date ">
                                            <input id="filterForm-makeDateE" class="form-control " size="16" type="text"
                                                value="" placeholder="结束日期" readonly>
                                            <span class="input-group-addon"><span
                                                    class="glyphicon glyphicon-calendar"></span></span>
                                        </div>
                                    </div>

                                </div>



                                <div class="form-group">
                                    <label class="col-sm-2 control-label"></label>

                                    <div class="col-sm-10">
                                        <button type="button" class="btn btn-default" data-dismiss="modal"
                                            id="closeFilter">关闭
                                        </button>
                                        <button type="button" class="btn btn-primary" id="confirmFilter">
                                            确定
                                        </button>

                                    </div>


                                </div>
                            </form>

                        </div>
                    </div>


                </div><!-- /.modal-content -->
            </div><!-- /.modal -->
        </div>
    </div>

    <!-- 导出模态框（Modal） -->
    <div class="modal fade" id="exportModal" tabindex="-1" role="dialog" aria-hidden="true">

        <div class="modal-dialog" style="width: 800px;">

            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
                        &times;
                    </button>
                    <h4 class="modal-title">
                        字段选择
                    </h4>
                </div>
                <div class="modal-body">

                    <div class="panel panel-info">
                        <div class="panel-body" style="height:600px">

                            <label class="col-sm-2 control-label">主表字段</label>
                            <div class="col-sm-4">

                                <div class="panel panel-default">
                                    <div class="panel-body" style="height:500px">

                                        <form class="form-horizontal" role="form" id='exportModelForm'>

                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="transferBillId"
                                                    checked>
                                                <label class="form-check-label">调拨单号</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="transferDate"
                                                    checked>
                                                <label class="form-check-label">调拨日期</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="outWarehouseId"
                                                    checked>
                                                <label class="form-check-label">拨出仓库编号</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="outWarehouseName"
                                                    checked>
                                                <label class="form-check-label">拨出仓库</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="inWarehouseId"
                                                    checked>
                                                <label class="form-check-label">拨入仓库编号</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="inWarehouseName"
                                                    checked>
                                                <label class="form-check-label">拨入仓库</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="operator"
                                                    checked>
                                                <label class="form-check-label">经手人</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="maker" checked>
                                                <label class="form-check-label">制单人</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="auditor" checked>
                                                <label class="form-check-label">审核人</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="makeDate"
                                                    checked>
                                                <label class="form-check-label">制单日期</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="auditDate"
                                                    checked>
                                                <label class="form-check-label">审核日期</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="status" checked>
                                                <label class="form-check-label">单据状态</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="remark" checked>
                                                <label class="form-check-label">备注</label>
                                            </div>

                                        </form>
                                    </div>
                                </div>

                            </div>

                            <label class="col-sm-2 control-label">子表字段</label>
                            <div class="col-sm-4">

                                <div class="panel panel-default">
                                    <div class="panel-body">
                                        <form class="form-horizontal" role="form" id='exportSubModelForm'>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="rowId" checked>
                                                <label class="form-check-label">序号</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="productId"
                                                    checked>
                                                <label class="form-check-label">产品编号</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="productName"
                                                    checked>
                                                <label class="form-check-label">产品名称</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="unit" checked>
                                                <label class="form-check-label">单位</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="num" checked>
                                                <label class="form-check-label">数量</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="subRemark"
                                                    checked>
                                                <label class="form-check-label">子表备注</label>
                                            </div>

                                        </form>
                                    </div>
                                </div>

                            </div>




                            <div class="col-sm-12">
                                <button type="button" class="btn btn-primary" id="doExportBtn">
                                    确认导出
                                </button>
                            </div>

                        </div>
                    </div>


                </div><!-- /.modal-content -->
            </div><!-- /.modal -->
        </div>
    </div>

    <!-- 打印预览模态框（Modal） -->
    <div class="modal fade" id="printViewModal">
        <div class="modal-dialog" style="width: 80%;">
            <div class="modal-content">
                <div class="modal-body">
                    <div id="printViewDiv">

                        <h2>弘讯科技股份有限公司调拨单</h2>
                        <hr>

                        <table id='printViewMainTable' style="border: 0ch; width: 100%;table-layout:fixed;">
                            <tbody>
                                <tr>
                                    <td width='80px'>调拨单号:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-transferBillId"></p>
                                    </td>
                                    <td width='80px'>调拨日期:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-transferDate"></p>
                                    </td>
                                </tr>
                                <tr>
                                    <td width='80px'>拨出仓库:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-outWarehouseName">
                                        </p>
                                    </td>
                                    <td width='80px'>拨入仓库:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-inWarehouseName"></p>
                                    </td>
                                </tr>
                                <tr>
                                    <td width='80px'>联系人:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-contact"></p>
                                    </td>
                                    <td width='80px'>移动电话:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-mobilePhone"></p>
                                    </td>
                                </tr>
                                <tr>
                                    <td width='80px'>出货方式:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-sendWay"></p>
                                    </td>
                                    <td width='80px'>经手人:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-operator"></p>
                                    </td>
                                </tr>

                            </tbody>
                        </table>



                        <hr>
                        <table id='printViewSubTable' class="table table-condensed">

                            <thead>
                                <tr>
                                    <th>序号</th>
                                    <th>产品编号</th>
                                    <th>产品名称</th>
                                    <th>单位</th>
                                    <th>数量</th>
                                </tr>
                            </thead>
                            <tbody>

                            </tbody>
                        </table>

                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-default" id="printCloseBtn">关闭
                    </button>
                    <button type="button" class="btn btn-primary" id="confirmPrintBtn">
                        确认打印
                    </button>
                </div>
            </div>
        </div>
    </div>



    <!-- 打印预览模态框（Modal） -->
    <!-- <div class="modal fade" id="listPrintViewModal">
        <div class="modal-dialog" style="width: 600px;">
            <div class="modal-content">
                <div class="modal-body">

                    <h2>弘讯科技股份有限公司调拨清单</h2>
                    <hr>

                    <div id="listPrintViewDiv">


                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-default" id="listPrintCloseBtn">关闭
                    </button>
                    <button type="button" class="btn btn-primary" id="listPrintConfirmBtn">
                        确认打印
                    </button>
                </div>
            </div>
        </div>
    </div> -->





    <!-- 模态框（Modal） -->
    <div class="modal fade" id="rp_transferbillsModal" tabindex="-1" role="dialog" aria-hidden="true"
        data-backdrop="static">

        <div class="modal-dialog" style="width: 75%;">

            <!-- 按钮触发模态框 -->
            <button class="btn btn-primary" data-toggle="modal" data-target="#rp_transferbillsModal"
                id="rp_transferbillsModalOpen" style="display: none;">
                开始演示模态框
            </button>
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
                        &times;
                    </button>
                    <h4 class="modal-title">
                        表单详情
                    </h4>


                </div>
                <div class="modal-body">


                    <!-- 表单面板 -->
                    <div class="panel panel-default">
                        <div class="panel-body" style="min-height: 550px;">


                            <form class="form-horizontal" role="form" id='rp_transferbillsForm'>

                                <input id="rp_transferbillsForm-DBID" type="hidden" value="">

                                <div class="form-group">
                                    <label class="col-sm-1 control-label"
                                        for="rp_transferbillsForm-transferBillId">调拨单号</label>
                                    <div class="col-sm-2">
                                        <p class="form-control-static" id="rp_transferbillsForm-transferBillId"></p>
                                    </div>

                                    <label class="col-sm-1 control-label"
                                        for="rp_transferbillsForm-transferDate">调拨日期:</label>
                                    <div class="col-sm-2 ">
                                        <div class="input-group date form_date ">
                                            <input id="rp_transferbillsForm-transferDate" class="form-control "
                                                size="16" type="text" value="" readonly>
                                            <span class="input-group-addon"><span
                                                    class="glyphicon glyphicon-calendar"></span></span>
                                        </div>
                                    </div>



                                    <label class="col-sm-1 control-label">拨出仓库</label>
                                    <div class="col-sm-2">
                                        <select id="rp_transferbillsForm-outWarehouseId"
                                            class="selectpicker form-control" data-live-search="true" data-size="5"
                                            data-dropup-auto="false">
                                        </select>
                                    </div>

                                    <label class="col-sm-1 control-label">拨入仓库</label>
                                    <div class="col-sm-2">
                                        <select id="rp_transferbillsForm-inWarehouseId"
                                            class="selectpicker form-control" data-live-search="true" data-size="5"
                                            data-dropup-auto="false">
                                        </select>
                                    </div>
                                </div>







                                <div class="form-group">

                                    <label class="col-sm-1 control-label text-warning"
                                        for="rp_transferbillsForm-operator">经手人</label>
                                    <div class="col-sm-5">
                                        <!-- <input class="form-control" id="rp_transferbillsForm-operator" type="text"> -->
                                        <select id="rp_transferbillsForm-operator" class="selectpicker"
                                            data-live-search="true" data-size="5">
                                        </select>
                                    </div>


                                    <label class="col-sm-1 control-label">单据状态</label>
                                    <div class="col-sm-5">
                                        <p class="form-control-static" id="rp_transferbillsForm-status"></p>
                                    </div>
                                </div>

                                <!-- <div class="form-group">
                                    <label class="col-sm-1 control-label" for="msgSwitch">短信提醒</label>
                                    <div class="col-sm-11">
                                        <input id="msgSwitch" type="checkbox">
                                    </div>
                                </div> -->


                                <div class="form-group">
                                    <label class="col-sm-1 control-label" for="rp_transferbillsForm-maker">制单人</label>
                                    <div class="col-sm-2">
                                        <p class="form-control-static" id="rp_transferbillsForm-maker"></p>
                                    </div>

                                    <label class="col-sm-1 control-label"
                                        for="rp_transferbillsForm-makeDate">制单日期</label>
                                    <div class="col-sm-2">
                                        <p class="form-control-static" id="rp_transferbillsForm-makeDate"></p>
                                    </div>

                                    <label class="col-sm-1 control-label" for="rp_transferbillsForm-auditor">审核人</label>
                                    <div class="col-sm-2">
                                        <select id="rp_transferbillsForm-auditor" class="selectpicker"
                                            data-live-search="true" data-size="5">
                                        </select>
                                    </div>

                                    <label class="col-sm-1 control-label"
                                        for="rp_transferbillsForm-auditDate">审核日期</label>
                                    <div class="col-sm-2">
                                        <p class="form-control-static" id="rp_transferbillsForm-auditDate"></p>
                                    </div>
                                </div>

                                <div class="form-group">
                                    <label class="col-sm-1 control-label">单据备注</label>
                                    <div class="col-sm-11">
                                        <input class="form-control" id="rp_transferbillsForm-remark" type="text">
                                    </div>
                                </div>

                            </form>

                            <!-- 子表 -->
                            <hr>

                            <div class="row clearfix">
                                <div class="col-md-12 column">

                                    <!-- 功能组按钮 -->
                                    <div class="well">
                                        <button id='btnAddProduct' type="button" class="btn btn-info">新增产品</button>
                                        <button id='btnDeleteProduct' type="button" class="btn btn-danger">删除产品</button>
                                        <!-- <button id='btnScan' type="button" class="btn btn-success">扫描新增</button> -->

                                    </div>

                                    <!-- 定义基础模块数据表 -->
                                    <table id="rp_transfersubbills" class=" table table-striped table-bordered"
                                        style="width:100%">
                                        <thead>
                                            <tr>
                                                <th>序号</th>
                                                <th>产品编号</th>
                                                <th>产品描述</th>
                                                <th>单位</th>
                                                <th>数量</th>
                                                <th>备注</th>
                                                <th>状态</th>
                                            </tr>
                                        </thead>
                                        <tbody></tbody>
                                    </table>

                                    <div class="panel panel-default" id='rp_transfersubbillsPanel'>
                                        <div class="panel-heading">
                                            <h3 class="panel-title">产品详情</h3>
                                        </div>
                                        <div class="panel-body">

                                            <form class="form-horizontal" role="form" id='rp_transfersubbillsForm'>

                                                <!-- <input id="rp_transfersubbillsForm-DBID" type="hidden" value=""> -->
                                                <input id="rp_transfersubbillsForm-transferBillId" type="hidden"
                                                    value="">

                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label">序号</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static"
                                                            id="rp_transfersubbillsForm-rowId">
                                                        </p>
                                                    </div>

                                                    <label class="col-sm-1 control-label">可用库存</label>
                                                    <div class="col-sm-3">
                                                        <p class="form-control-static" id="stockNum">
                                                        </p>
                                                    </div>
                                                </div>

                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label">状态</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static"
                                                            id="rp_transfersubbillsForm-status">
                                                        </p>
                                                    </div>
                                                </div>


                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label">产品编号</label>
                                                    <div class="col-sm-5">
                                                        <select id="rp_transfersubbillsForm-productId"
                                                            class="selectpicker form-control" data-live-search="true"
                                                            data-size="5" data-dropup-auto="false">
                                                        </select>
                                                    </div>
                                                </div>

                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label">产品名称</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static"
                                                            id="rp_transfersubbillsForm-productName">
                                                        </p>
                                                    </div>

                                                    <label class="col-sm-1 control-label">产品描述</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static"
                                                            id="rp_transfersubbillsForm-productDescription">
                                                        </p>
                                                    </div>
                                                </div>

                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label">产品类别</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static"
                                                            id="rp_transfersubbillsForm-productClass">
                                                        </p>

                                                    </div>

                                                    <label class="col-sm-1 control-label">系统分类</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static"
                                                            id="rp_transfersubbillsForm-systemType">
                                                        </p>
                                                    </div>

                                                </div>

                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label">单位</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static"
                                                            id="rp_transfersubbillsForm-unit">
                                                        </p>

                                                    </div>

                                                    <label class="col-sm-1 control-label">数量</label>
                                                    <div class="col-sm-5">
                                                        <input class="form-control" id="rp_transfersubbillsForm-num"
                                                            type="text">
                                                    </div>

                                                </div>


                                                <div class="form-group">

                                                    <label class="col-sm-1 control-label">备注</label>
                                                    <div class="col-sm-11">
                                                        <input class="form-control" id="rp_transfersubbillsForm-remark"
                                                            type="text">
                                                    </div>
                                                </div>

                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label"></label>
                                                    <div class="col-sm-5">
                                                        <button type="button" class="btn btn-primary"
                                                            id="btnProductSave">
                                                            保存
                                                        </button>

                                                    </div>
                                                </div>

                                            </form>


                                        </div>
                                    </div>

                                </div>
                            </div>

                            <!-- 扫描模态框（Modal） -->

                            <!-- <div class="modal fade" id="scanModal">
                                <div class="modal-dialog" style="width: 675px;">
                                    <div class="modal-content">

                                        <div class="modal-body">请将产品二维码放置于摄像头内
                                            <div><button id='btnScanStop' type="button"
                                                    class="btn btn-info">停止扫描</button></div>
                                            <div style="width:640;height:480px"><video id="preview"></video>
                                            </div>

                                        </div>

                                    </div>
                                </div>
                            </div> -->


                        </div>
                    </div>

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal"
                        id="rp_transferbillsModalClose">关闭
                    </button>

                    <button type="button" class="btn btn-primary" id="rp_transferbillsSave">
                        保存单据
                    </button>
                </div>
            </div><!-- /.modal-content -->
        </div><!-- /.modal -->
    </div>

</div>



<script type="text/javascript">
    //申请表主表dataTable column 数组

    const mainColumnsArray = [{
        data: 'transferBillId'
    }, {
        data: 'outWarehouseName',
    }, {
        data: 'inWarehouseName',
    }, {
        data: 'maker',
    }, {
        data: 'makeDate',
    }, {
        data: 'auditor',
    }, {
        data: 'auditDate',
    }, {
        data: 'remark',
    }, {
        data: 'status',
    }];

    //申请表子表dataTable column 数组
    const subColumnsArray = [{
        data: 'rowId',
    }, {
        data: 'productId',
    }, {
        data: 'productDescription',
    }, {
        data: 'unit',
    }, {
        data: 'num',
    }, {
        data: 'remark',
    }, {
        data: 'status',
    }]





    //填写产品信息功能
    const fillProductInfo = async (productId) => {
        let r1 = await getDataBySql({
            sql: 'getProduct',
            params: {
                filter: 'productId="' + productId + '"'
            }
        })

        console.log('r1555555555:' + JSON.stringify(r1));
        let r2 = r1[0]
        delete r2.productId;
        // console.log('r2:' + JSON.stringify(r2));
        fillFormInputs({
            formId: 'rp_transfersubbillsForm',
            params: r2
        })

        // $('#rp_transfersubbillsForm-productId').selectpicker('val',r1[0].productId)

    }



    //选定产品修改事件
    const addProductListener = () => {
        // $('#rp_transfersubbillsForm-productId').unbind();
        $('#rp_transfersubbillsForm-productId').change(async function () {
            let productId = $('#rp_transfersubbillsForm-productId option:selected')
                .val()
            if (productId) {

                $('#rp_transfersubbillsForm-productId').selectpicker('refresh');
                showStockNum();
                fillProductInfo(productId);

            }
        });

    }
    //仓库选择修改事件
    const addOutWarehouseListener = () => {
        // $('#rp_transfersubbillsForm-warehouseId').unbind();
        $('#rp_transferbillsForm-outWarehouseId').change(async function () {
            let warehouseId = $('#rp_transferbillsForm-outWarehouseId option:selected')
                .val()

            loadBootStrapSelector({
                elementId: 'rp_transfersubbillsForm-productId',
                sqlParams: {
                    sql: 'getStoreProductSelector',
                    params: {
                        filter: 'warehouseId ="' + warehouseId + '"'
                    }
                },
            })


            // $('#rp_transferbillsForm-outWarehouseId').selectpicker('refresh');
            let productId = $('#rp_transfersubbillsForm-productId option:selected').val();
            if (warehouseId && productId) {
                showStockNum();
            }
        });
    }



    //创建调拨单新单号
    const createTransferBillId = async () => {
        let r1 = await getDataBySql({
            sql: "getTransferBillsNum",
            params: {
                filter: 'transferBillId like "M-%" and makeDate="' + currentDate() + '"'
            }
        })
        // console.log('r1:' + r1);
        let r2 = r1[0].billsNum
        let r3 = parseInt(r2) + 1;

        let r4 = PrefixInteger(r3, 3);
        // console.log('r4:' + r4);
        let r5 = dateFormat(currentDate(), 'yyyyMMdd');

        let transferBillId = 'M-' + r5 + r4;
        $("#rp_transferbillsForm-transferBillId").text(transferBillId);
    }



    //添加主表监听器,定义双击打开事件

    const addMainTableListener = () => {

        //解绑原事件
        $('#rp_transferbills tbody').unbind();

        //定义主表单击击事件
        $('#rp_transferbills tbody').on('click', 'tr', async function () {
            //设定选中
            let table = $('#rp_transferbills').DataTable();

            if ($(this).hasClass('selected')) {
                $(this).removeClass('selected');
                //清空选中单据数据
                mainTableData = {}
                $('#subTablePanel').hide();
            } else {
                table.$('tr.selected').removeClass('selected');
                $(this).addClass('selected');

                let rowSelect = table.row('.selected').data();
                // console.log("rowSelect:" + JSON.stringify(rowSelect));

                mainTableData = rowSelect; //刷新全局主表对象数据

                await loadMainBillSubTable(rowSelect.transferBillId)

                //打开面板
                $('#subTablePanel').show();

            }

        });


        //定义主表双击事件
        $('#rp_transferbills tbody').on('dblclick', 'tr', async function () {

            //打开关联信息,打印功能
            $("#accordion").show();

            // $("#listPrintBtn").show();
            // $("#rp_transferbillsPrint").show();

            //设定选中
            let table = $('#rp_transferbills').DataTable();
            table.$('tr.selected').removeClass('selected');
            $(this).addClass('selected');
            let dataSelect = table.row('.selected').data();

            mainTableData = dataSelect; //刷新全局主表对象数据

            // console.log('mainTableData', mainTableData);

            //解除客户监听事件
            $('#rp_transferbillsForm-customerShortName').unbind();

            //初始化并填写form内inputs
            initAndFillFormInputs({
                formId: 'rp_transferbillsForm',
                params: dataSelect
            })

            // let r1 = await getDataBySql({
            //     sql: 'getCustomers',
            //     params: {
            //         filter: 'customerShortName="' + dataSelect.customerShortName + '"'
            //     }
            // })

            // // console.log('r1:' + JSON.stringify(r1));

            // customerData = r1[0]; //将查询结果赋给全局变量

            //关闭子表panel
            $('#rp_transfersubbillsPanel').hide();

            //加载子表记录表数据

            loadBillSubTable(dataSelect.transferBillId);



            //打开面板
            $('#rp_transferbillsModal').modal('show');

        });

    }

    //主表加载数据函数;

    const loadBillMainTable = async () => {

        //显示过滤模式
        switch (true) {
            case mainTableFilter === 'to_days(makeDate) = to_days(now())':
                $('#btnFilterToday').addClass('active');
                $('#btnFilterUnAudit').removeClass('active');
                $('#btnFilterDiy').removeClass('active');

                break;
            case mainTableFilter === 'status ="待审核"':
                $('#btnFilterToday').removeClass('active');
                $('#btnFilterUnAudit').addClass('active');
                $('#btnFilterDiy').removeClass('active');
                break;
            default:
                $('#btnFilterToday').removeClass('active');
                $('#btnFilterUnAudit').removeClass('active');
                $('#btnFilterDiy').addClass('active');
                break;
        }

        //初始化主表
        loadBillDataTable({
            elementId: 'rp_transferbills',
            sqlParams: {
                sqlId: 'sqlTransferBills',
                params: {
                    filter: mainTableFilter,
                    orderBy: 'billSaveTimeStamp desc'
                }
            },
            dtParams: {
                // bAutoWidth: false,//取消自动宽度
                columns: mainColumnsArray,
                // scrollX: true,
                // autoWidth: true
            }
        })
        //清空选项值
        $('.dataTables_filter input').val('')

        //自定义样式调整
        $('.dataTables_filter input').attr({
            'name': 'search',
            'size': 45
        });
        $('.dataTables_filter input').attr({
            'name': 'search',
            'placeholder': '可用表属性过滤'
        });

        // //添加搜索框监听事件,值变动自动隐藏主页附表
        // $('#rp_transferbills').on('search.dt', function () {
        //     console.log(1111);
        //     $('#subTablePanel').hide();
        // })


        // //隐藏搜索值数据列,bStateSave 会导致列中visible 属性失效,采用指定隐藏方案
        // let table = $('#rp_transferbills').DataTable();
        // table.column('last').visible(false)

        addMainTableListener();

        //隐藏子表
        $('#subTablePanel').hide();
    }

    //添加子表监听器,定义双击展开事件
    const addSubTableListener = () => {
        let subTable = $('#rp_transfersubbills').DataTable();

        //解绑原事件
        $('#rp_transfersubbills tbody').unbind();

        //定义子表双击事件
        $('#rp_transfersubbills tbody').on('click', 'tr', function () {

            if ($(this).hasClass('selected')) {
                $(this).removeClass('selected');

                $('#rp_transfersubbillsPanel').hide();
            } else {
                subTable.$('tr.selected').removeClass('selected');
                $(this).addClass('selected');

                let rowSelect = subTable.row('.selected').data();
                // console.log("rowSelect:" + JSON.stringify(rowSelect));


                //初始化并填写form内inputs
                initAndFillFormInputs({
                    formId: 'rp_transfersubbillsForm',
                    params: rowSelect
                })

                //打开面板
                $('#rp_transfersubbillsPanel').show();

                // //选定产品修改事件
                // addProductListener();
            }

        });
    }





    //用数组重载子表
    const loadDataSubTable = async ({
        elementId,
        dataArray
    }) => {

        //加载表格
        await loadBillDataTable({
            elementId: elementId,
            dtParams: {
                dtConfig: 'dtConfigData',
                data: dataArray,
                columns: subColumnsArray,
                // scrollX: true,
                // bAutoWidth: true
            }
        })

        //如数据为0,清空tbody 避免直接获取size =1;
        if (dataArray.length === 0) {
            $('#' + elementId + ' tbody').html("");
        }


        //添加子表双击事件
        addSubTableListener();
    }

    //加载展开选定表子表关联数据

    const loadBillSubTable = async (transferBillId) => {


        loadDataSubTable({
            elementId: 'rp_transfersubbills',
            dataArray: subTableData
        });

        // loadDataSubTable({
        //     elementId: 'rp_transferbillsSub',
        //     dataArray: subTableData
        // });

    }

    //加载展开选定表子表关联数据

    const loadMainBillSubTable = async (transferBillId) => {
        let dataArray = await getDataBySql({
            sql: 'sqlTransferSubBills',
            params: {
                filter: 'transferBillId="' + transferBillId + '"'
            }
        })
        //将表数据同步到共用数组
        subTableData = dataArray;
        // console.log(subTableData);


        loadDataSubTable({
            elementId: 'rp_transferbillsSub',
            dataArray: subTableData
        });
    }




    //保存调拨单函数
    const saveTransferBill = async () => {

        let mainFormData = getPostFormData({
            formId: 'rp_transferbillsForm'
        })

        //给主表数据添加,maker 和makeDate日期;新增状态等待单号检测完成,制单人为空则为新增状态

        if (!mainFormData.data[0].maker) {
            await autoUpdateTransferBillId();
            //刷新调拨单号
            mainFormData.data[0].transferBillId = $("#rp_transferbillsForm-transferBillId").text();

            mainFormData.data[0].status = '待审核';
            mainFormData.data[0].maker = sessionName;
            mainFormData.data[0].makeDate = currentDate();
        }
        mainFormData.data[0].outWarehouseName = $('#rp_transferbillsForm-outWarehouseId option:selected')
            .text()
        mainFormData.data[0].inWarehouseName = $('#rp_transferbillsForm-inWarehouseId option:selected')
            .text()

        // console.log("subTableData:" + JSON.stringify(subTableData))

        for (let n of subTableData) {
            n.transferBillId = $("#rp_transferbillsForm-transferBillId").text();
            if (n.status === '新增') {
                n.status = '预调拨'
            }
        }

        let r1 = await postDBData({
            sql: 'replace',
            params: mainFormData
        })

        let r2 = await postDBData({
            sql: 'replace',
            params: {
                tableId: 'rp_transfersubbills',
                data: subTableData
            }
        })

        let PIDArr = [];
        for (const n of subTableData) {
            PIDArr.push({
                PID: n.productId,
                warehouseId: mainFormData.data[0].inWarehouseId
            })
        }

        let r3 = await initWarehousePID({
            PIDArr: PIDArr
        })

        // console.log('saveTransferBill r1:' + JSON.stringify(r1));
        // console.log('saveTransferBill r2:' + JSON.stringify(r2));

        if (r1 && r2 && r3) {
            alert('保存成功!')
        } else {
            alert('出现错误!')
        }

        // //更新客户信息功能

        // // console.log('customerData:' + JSON.stringify(customerData));
        // // console.log('mainFormData:' + JSON.stringify(mainFormData));

        // //定义当前表内实际填写客户数据
        // let billCustomerData = {
        //     customerId: mainFormData.data[0].customerId,
        //     // customerArea: mainFormData.data[0].customerArea,
        //     customerName: mainFormData.data[0].customerName,
        //     contact: mainFormData.data[0].contact,
        //     mobilePhone: mainFormData.data[0].mobilePhone,
        //     customerBelongShort: mainFormData.data[0].customerBelongShort,
        //     // isInland: mainFormData.data[0].isInland
        // }

        // //判断当前表内客户数据是否存在更新;
        // let compareResult = true;
        // for (const p in billCustomerData) {
        //     if (billCustomerData[p] !== customerData[p]) {

        //         // console.log(p + ':' + 'billCustomerData[p]' + billCustomerData[p] + ',customerData[p]:' +
        //         //     customerData[p] + '不同');

        //         compareResult = false;
        //         break;
        //     }
        // }

        // //存在不同则更新客户信息
        // if (!compareResult) {

        //     if (confirm('是否更新该客户基础信息?')) {
        //         let rc = await postDBData({
        //             sql: 'update',
        //             params: {
        //                 tableId: 'rp_customers',
        //                 data: [billCustomerData],
        //                 key: 'customerId'
        //             }
        //         })
        //         if (rc.affectedRows > 0) {
        //             alert('更新客户信息成功!')
        //         }

        //     }
        // }

        // $('#rp_transferbillsModal').modal('hide');多层modal存在失效问题,改用click关闭
        $('#rp_transferbillsModalClose').click()

        loadBillMainTable();


    }


    //检查需求单Id函数,防止多人同时填单时出现的覆盖问题

    const checkTransferBillId = async () => {
        let transferBillId = $("#rp_transferbillsForm-transferBillId").text();

        let r1 = await getDataBySql({
            sql: 'getTransferBillsNum',
            params: {
                filter: 'transferBillId="' + transferBillId + '"'
            }
        })

        let r2 = r1[0].billsNum;
        // console.log("checkTransferBillId:" + r2);

        let o = r2 > 0 ? false : true;
        return o;

    }

    //更新需求单号加1
    const updateTransferBillId = () => {
        let transferBillId = $("#rp_transferbillsForm-transferBillId").text();
        if (transferBillId) {
            transferBillIdArr = transferBillId.split('-');
            let newBillId = transferBillIdArr[0] + '-' + (parseInt(transferBillIdArr[1]) + 1);
            $("#rp_transferbillsForm-transferBillId").text(newBillId);
        }
    }

    //递归更新单号直到不存在冲突单号;

    const autoUpdateTransferBillId = async () => {
        let r = await checkTransferBillId();
        if (r) {
            return true
        } else {
            // console.log('fail:' + $("#rp_transferbillsForm-transferBillId").text());
            updateTransferBillId()
            // console.log('fail11:' + $("#rp_transferbillsForm-transferBillId").text());
            return await autoUpdateTransferBillId();
        }
    }




    //更新所选产品即时库存
    const showStockNum = async () => {
        let stockNum = 0;
        let productId = $('#rp_transfersubbillsForm-productId option:selected').val();
        let warehouseId = $('#rp_transferbillsForm-outWarehouseId option:selected').val();
        // console.log(warehouseId);

        if (productId && warehouseId) {
            stockNum = await getStockNum(productId, warehouseId);
        }
        $('#stockNum').text(stockNum)
    }




    //检测库存状态subTableData=[{partId,num,numStatus}]
    const checkSubTableStock = async (subTableData) => {
        //1.check
        console.log('checkSubTableStock', subTableData);
        if (subTableData.length < 1) {
            alert('参数错误,子表不存在产品')
            return false
        }

        //2.do
        let PIDArr = [];
        for (const n of subTableData) {
            PIDArr.push({
                PID: n.productId,
                warehouseId: n.warehouseId
            });
        }

        let stockNums = await getStockNums(PIDArr)
        console.log('stockNums', stockNums);

        for (const n of subTableData) {
            for (const m of stockNums) {
                if (n.productId === m.PID && n.num > m.stockNum) {
                    alert('产品编号:' + n.productId + '在' + n.warehouseName + '中库存不足,请补充库存后重试!')
                    return false
                }
            }
        }

        //3.return
        return {
            result: true,
            stockNums: stockNums
        }

    }

    function selectFile() {
        $("#file")[0].click();
    }

    //去除excel生成json中的中文属性
    const excelJsonArr2DivJsonArr = (excelJsonArr, rules) => {
        for (let n of excelJsonArr) {
            n = replaceObjParamsName(n, rules)
        }
        return excelJsonArr;
    }

    const addFileReaderListener = () => {
        $("#file")[0].addEventListener('change', function (e) {

            //check-检查文件数量
            var files = e.target.files;
            if (files.length == 0) return;
            var f = files[0];

            //check-检查文件格式
            if (!/\.xlsx$/g.test(f.name)) {
                alert('仅支持读取xlsx格式！');
                return;
            }

            //do-读取数据
            readWorkbookFromLocalFile(f, async function (workbook) {
                let excelData = readWorkbook(workbook);
                let storeData = excelJsonArr2DivJsonArr(excelData, {
                    transferBillId: '调拨单号',
                    transferDate: '调拨日期',
                    outWarehouseId: '拨出仓库编号',
                    outWarehouseName: '拨出仓库',
                    inWarehouseId: '拨入仓库编号',
                    inWarehouseName: '拨入仓库',
                    operator: '经手人',
                    maker: '制单人',
                    auditor: '审核人',
                    remark: '备注',
                    rowId: '行号',
                    productId: '产品编号',
                    productName: '产品名称',
                    unit: '单位',
                    num: '数量',
                    subRemark: '子表备注'
                })

                console.log('storeData', storeData);

                //c-校验单号规则
                let billsIdArr = [];
                for (const n of storeData) {
                    if (n.transferBillId.indexOf('PM-') === -1) {
                        alert('批量导入单号必须以 PM- 开始,请检查!')
                        return
                    }
                    billsIdArr.push(n.transferBillId)
                }

                //c-检查重复单号
                let billIdRange = getRangeString(billsIdArr);
                // console.log("billsIdArr:" + JSON.stringify(billsIdArr));

                let statusArr = await getDataBySql({
                    sql: 'sqlTransferBills',
                    params: {
                        filter: 'transferBillId in ' + billIdRange
                    }
                })
                console.log('statusArr', statusArr);

                let dupBillIds = "";
                if (statusArr.length > 0) {
                    for (const n of statusArr) {
                        dupBillIds = dupBillIds + n.transferBillId + ',';
                    }
                    dupBillIds = dupBillIds.substring(0, dupBillIds.length - 1);
                    alert('以下单号' + dupBillIds + '存在重复单号,请检查!')
                    return
                }

                console.log('storeData', storeData);
                //do-生成主表,子表数据
                let transferBillsArr = [];
                let transferSubBillsArr = [];

                for (const n of storeData) {

                    transferBillsArr.push({
                        transferBillId: n.transferBillId,
                        transferDate: n.transferDate,
                        outWarehouseId: n.outWarehouseId,
                        outWarehouseName: n.outWarehouseName,
                        inWarehouseId: n.inWarehouseId,
                        inWarehouseName: n.inWarehouseName,
                        operator: n.operator,
                        maker: n.maker,
                        makeDate: currentDate(),
                        auditor: n.auditor,
                        status: '待审核',
                        remark: n.remark
                    })

                    transferSubBillsArr.push({
                        transferBillId: n.transferBillId,
                        rowId: n.rowId,
                        productId: n.productId,
                        productName: n.productName,
                        unit: n.unit,
                        num: n.num,
                        status: '预调拨',
                        remark: n.subRemark
                    })
                }
                //do-去重处理
                // inBillsArr = Array.from(new Set(inBillsArr))仅可用于简单数组去重
                var obj = {};
                transferBillsArr = transferBillsArr.reduce(function (item, next) {
                    obj[next.transferBillId] ? '' : obj[next.transferBillId] = true &&
                        item.push(next);
                    return item
                }, [])

                //子表不去重
                // var subObj = {};
                // inSubBillsArr = inSubBillsArr.reduce(function (item, next) {
                //     subObj[next.billId] ? '' : subObj[next.billId] = true && item.push(
                //         next);
                //     return item
                // }, [])
                console.log('transferBillsArr', transferBillsArr);
                console.log('transferSubBillsArr', transferSubBillsArr);

                //do-导入数据库
                let rTranferBills = await postDBData({
                    sql: 'insert',
                    params: {
                        tableId: 'rp_transferbills',
                        data: transferBillsArr
                    }
                })

                let rTranferSubBills = await postDBData({
                    sql: 'insert',
                    params: {
                        tableId: 'rp_transfersubbills',
                        data: transferSubBillsArr
                    }
                })

                //do-初始化库存
                let PIDArr = [];
                for (const n of transferSubBillsArr) {
                    PIDArr.push({
                        PID: n.productId,
                        warehouseId: n.warehouseId
                    })
                }

                let newPIDArr = []
                for (let i = 0; i < PIDArr.length; i++) {
                    let isNew = true
                    for (let j = 0; j < newPIDArr.length; j++) {
                        if (PIDArr[i].PID === newPIDArr[j].PID && PIDArr[i].warehouseId ===
                            newPIDArr[j].warehouseId) {
                            isNew = false;
                            break;
                        }
                    }
                    if (isNew) {
                        newPIDArr.push(PIDArr[i])
                    }
                }

                //初始化库存记录
                let rInitPID = await initWarehousePID({
                    PIDArr: newPIDArr
                })

                if (rTranferBills.affectedRows > 0 && rTranferSubBills.affectedRows > 0 &&
                    rInitPID) {
                    alert('批量导入成功')
                } else {
                    alert('批量导入出错')
                }

                //return-刷新主界面
                loadBillMainTable()
            });
        });
    }


    $(document).ready(function () {

        // alert(parseInt(''))
        //定义全局变量,当前用户名
        sessionName = "<%-locals.session.yjUser.Name%>";
        //定义主表全局变量临时数据对象
        mainTableData = {};
        //定义子表全局变量临时数据数组
        subTableData = [];


        //主表过滤器
        mainTableFilter = '';


        //初始化子表
        loadDataSubTable({
            elementId: 'rp_transfersubbills',
            dataArray: subTableData
        });

        //过滤器当天按钮
        $('#btnFilterToday').click(function () {
            mainTableFilter = 'to_days(makeDate) = to_days(now())';
            //初始化主表
            loadBillMainTable();

        })

        $('#btnFilterToday').click(); //默认开启当天过滤

        //过滤器待审核
        $('#btnFilterUnAudit').click(function () {
            mainTableFilter = 'status ="待审核"';
            //初始化主表
            loadBillMainTable();
        })

        //过滤器自定义按钮
        $('#btnFilterDiy').click(function () {
            //打开面板
            $('#filterModal').modal('show');
            clearFormInputs({
                formId: 'filterForm'
            })

        })

        //过滤器确认按钮

        $('#confirmFilter').click(function () {
            //获取filterform内数据
            let filter = '';


            //制单日期过滤
            let makeDateB = $('#filterForm-makeDateB').val();
            let makeDateE = $('#filterForm-makeDateE').val();

            if (makeDateB) {
                filter = filter + ' and makeDate>="' + makeDateB + '"'
            }

            if (makeDateE) {
                filter = filter + ' and makeDate<="' + makeDateE + '"'
            }



            //去除filter中开始的and关键字避免sql错误;

            if (filter) {
                filter = filter.substring(4, filter.length);
            }

            mainTableFilter = filter

            $('#closeFilter').click();
            loadBillMainTable()

        })



        //新增调拨单按钮
        $('#btnTransferBillNew').click(function () {

            //清空form数据
            mainTableData = {}
            subTableData = {}

            clearFormInputs({
                formId: 'rp_transferbillsForm'
            })

            //清空原有选项
            $('#customerContacts').empty();
            $('#customerContacts').selectpicker('destroy');


            //清空子表及全局变量
            subTableData = [];
            $('#rp_transfersubbills tbody').html("");


            //打开面板
            $('#rp_transfersubbillsPanel').hide();


            //清空modal数据
            clearFormInputs({
                formId: 'rp_transferbillsForm'
            })


            createTransferBillId();

            $("#rp_transferbillsForm-outWarehouseId").selectpicker('val', 'W1')
            $("#rp_transferbillsForm-sendWay").selectpicker('val', '邮寄过来')
            $("#rp_transferbillsForm-transferDate").val(currentDate());

            //在保存时赋值
            // $("#rp_transferbillsForm-maker").text(sessionName);
            // $("#rp_transferbillsForm-makeDate").text(currentDate());



            //显示/隐藏
            //关闭关联信息,打印按钮
            $("#accordion").hide();

            // $("#listPrintBtn").hide();
            // $("#rp_transferbillsPrint").hide();

            //打开面板
            $('#rp_transferbillsModal').modal('show');

        })

        //审核调拨单按钮
        $('#btnTransferBillAudit').click(async function () {
            //check-单据选中状态
            if (!mainTableData.DBID) {
                alert('请先选中需要审核的调拨单!')
                return
            }
            //check-单据审核状态
            if (mainTableData.status !== '待审核') {
                alert('该调拨单非待审核状态,无法审核!')
                return
            }

            //check-单据审核人
            if (mainTableData.auditor !== sessionName) {
                alert('当前账号非审核人,无法审核!')
                return
            }
            // //check-即时库存及领料数量
            // let stockResult = await checkSubTableStock(subTableData);
            // if (!stockResult.result) {
            //     return
            // }
            // console.log('stockResult', stockResult);

            //do-系统出库
            let outMainTable = {
                billFrom: '调拨单',
                billId: mainTableData.transferBillId,
                customerId: 'S_KFB',
                customerShortName: '客服部仓库',
                customerName: '客服部仓库',
                customerBelongShort: '',
                contact: '',
                // mobilePhone: mainTableData.mobilePhone,
                // workPhone: mainTableData.workPhone,
                // sendWay: mainTableData.sendWay,
                // address: mainTableData.address,
                operator: mainTableData.operator,
                maker: mainTableData.maker,
                makeDate: mainTableData.makeDate,
                auditor: mainTableData.auditor,
                auditDate: currentDate(),
                status: '已审核',
                remark: mainTableData.remark,
            };
            let outSubTable = [];
            for (const n of subTableData) {
                outSubTable.push({
                    billId: n.transferBillId,
                    rowId: n.rowId,
                    productId: n.productId,
                    productName: n.productName,
                    productDescription: n.productDescription,
                    productClass: n.productClass,
                    systemType: n.systemType,
                    warehouseId: mainTableData.outWarehouseId,
                    warehouseName: mainTableData.outWarehouseName,
                    unit: n.unit,
                    num: n.num,
                    status: '已出',
                    remark: n.remark,
                    actType: '调拨单',
                    rpBillId: mainTableData.transferBillId
                })
            }

            let r1 = await autoOutStoreBills({
                mainTable: outMainTable,
                subTable: outSubTable
            })

            console.log('r1', r1);


            //do-系统入库
            if (!r1) {
                alert('出库失败!')
                return;
            }

            let inMainTable = {
                billFrom: '调拨单',
                billId: mainTableData.transferBillId,
                customerId: 'S_KFB',
                customerShortName: '客服部仓库',
                customerName: '客服部仓库',
                customerBelongShort: '',
                contact: '',
                // mobilePhone: mainTableData.mobilePhone,
                // workPhone: mainTableData.workPhone,
                // sendWay: mainTableData.sendWay,
                // address: mainTableData.address,
                operator: mainTableData.operator,
                maker: mainTableData.maker,
                makeDate: mainTableData.makeDate,
                auditor: mainTableData.auditor,
                auditDate: currentDate(),
                status: '已审核',
                remark: mainTableData.remark,
            };
            let inSubTable = [];
            for (const n of subTableData) {
                let addObj = {
                    billId: n.transferBillId,
                    rowId: n.rowId,
                    productId: n.productId,
                    productName: n.productName,
                    productDescription: n.productDescription,
                    productClass: n.productClass,
                    systemType: n.systemType,
                    warehouseId: mainTableData.inWarehouseId,
                    warehouseName: mainTableData.inWarehouseName,
                    unit: n.unit,
                    num: n.num,
                    status: '已入',
                    remark: n.remark,
                    actType: '调拨单',
                    rpBillId: mainTableData.transferBillId
                }

                if (addObj.warehouseId === 'W3') {
                    addObj.scrapStatus = '待报废'
                }
                inSubTable.push(addObj)
            }

            let r2 = await autoInStoreBills({
                mainTable: inMainTable,
                subTable: inSubTable
            })

            if (!r2) {
                alert('入库失败!')
                return;
            }

            //do-更新调拨单主表

            let billUpdateData = {
                transferBillId: mainTableData.transferBillId,
                auditDate: currentDate(),
                status: '已审核'

            }

            let r3 = await postDBData({
                sql: 'update',
                params: {
                    tableId: 'rp_transferbills',
                    data: [billUpdateData],
                    key: 'transferBillId'
                }
            })

            //do-更新调拨单子表状态

            for (let n of subTableData) {
                if (n.status === '预调拨') {
                    n.status = '已调拨'
                }

                // if (mainTableData.inWarehouseId === 'W3') {
                //     n.scrapStatus = '待报废'
                // }
            }

            let r4 = await postDBData({
                sql: 'replace',
                params: {
                    tableId: 'rp_transfersubbills',
                    data: subTableData
                }
            })

            //return-操作成功信息
            if (r3 && r4) {
                alert('审核成功,系统已自动生成相应出库单,入库单!')
            } else {
                alert('审核失败')
            }
            //return-刷新主表
            loadBillMainTable();




        })


        //删除调拨单按钮
        $('#btnTransferBillDelete').click(async function () {

            //check-检查单据选中
            if (!mainTableData.transferBillId) {
                alert('请先选中需要删除的调拨单')
                return
            }

            //check-检查单据状态
            if (mainTableData.status !== '待审核') {
                alert('只允许删除待审核调拨单')
                return
            }

            //check-检查子表状态
            for (const n of subTableData) {
                if (n.status !== '预调拨') {
                    alert('存在非预调拨状态的物料,无法删除!')
                    return
                }
            }

            //check-提醒删除风险
            if (!confirm('删除后无法恢复,确认删除?')) {
                return;
            }

            // let resultData = await checkDeleteBills();
            // if (!resultData.result) {
            //     return;
            // }


            // console.log(mainTableData);
            // console.log(subTableData);


            //do-删除单据

            let mainDBIDArr = [mainTableData.DBID]
            let subDBIDArr = [];
            for (const n of subTableData) {
                subDBIDArr.push(n.DBID)
            }

            let r1 = await postDBData({
                sql: 'delete',
                params: {
                    tableId: 'rp_transferbills',
                    data: mainDBIDArr
                }
            })

            let r2 = await postDBData({
                sql: 'delete',
                params: {
                    tableId: 'rp_transfersubbills',
                    data: subDBIDArr
                }
            })

            //return-提醒并重新加载表单
            if (r1.affectedRows && r2.affectedRows) {
                alert('删除成功!')
            } else {
                alert('出现错误!')
            }
            loadBillMainTable();


        })



        // 扫描按钮
        $('#btnScan').click(function () {

            let scanner = new Instascan.Scanner({
                video: document.getElementById('preview')
            });
            scanner.addListener('scan', async function (productId) {
                // console.log('productId:' + productId);
                let productData = await getDataBySql({
                    sql: 'getProduct',
                    params: {
                        filter: 'productId="' + productId + '"'
                    }
                })
                // console.log("productData:" + JSON.stringify(productData));
                let rowNum = $("#rp_transfersubbills tbody tr").size();
                let rowData = {};

                rowData.rowId = rowNum + 1;
                rowData.status = '新增'
                rowData.productId = productData[0].productId;
                rowData.productName = productData[0].productName;
                rowData.productDescription = productData[0].productDescription;
                rowData.factoryNo = '11111'
                rowData.origin = 'nb'
                rowData.isRework = '否';
                rowData.urgent = '否';
                rowData.inWarranty = '否';
                rowData.productFrom = '终端客户';
                rowData.productBelong = '客户 ';

                // console.log("rowData:" + JSON.stringify(rowData));

                subTableData.push(rowData);
                //重新加载子表
                loadDataSubTable({
                    elementId: 'rp_transfersubbills',
                    dataArray: subTableData
                });

                // alert('productId:' + productId);
                // $('#rp_transfersubbillsPanel').show();


                scanner.stop().then(function () {
                    // alert('stoped');
                    $('#scanModal').modal('hide')
                });
            });
            Instascan.Camera.getCameras().then(function (cameras) {
                if (cameras.length > 0) {
                    scanner.start(cameras[0]).then(function () {
                        // alert('start');
                    }).catch(function (err) {
                        alert('err:' + err);
                    });
                } else {
                    console.error('No cameras found.');
                }
            }).catch(function (e) {
                console.error(e);
                alert("系统错误,请检查摄像头设备!")

            });

            // 停止扫描按钮功能定义
            $('#btnScanStop').unbind();
            $('#btnScanStop').click(function () {
                scanner.stop()
                $('#scanModal').modal('hide')
            })

            $('#scanModal').modal('show');
        })



        // 新增填单按钮
        $('#btnAddProduct').click(function () {
            let subTable = $('#rp_transfersubbills').DataTable();
            //删除子表选中状态
            // subTable.$('tr.selected').removeClass('selected');

            subTable.rows().deselect();

            //清空panel数据
            clearFormInputs({
                formId: 'rp_transfersubbillsForm'
            })
            //打开面板
            $('#rp_transfersubbillsPanel').show();

            //设置默认值

            $("#rp_transfersubbillsForm-status").text('新增')
            $("#rp_transfersubbillsForm-productId").selectpicker('val', '')
            $("#rp_transfersubbillsForm-warehouseId").selectpicker('val', 'W1')
            $("#rp_transfersubbillsForm-num").val(1)

        })

        //保存产品按钮

        $('#btnProductSave').click(async function () {

            //check-检查调拨单状态
            if (mainTableData.status === '已审核') {
                alert('当前调拨单已审核,不允许修改!')
                return
            }

            //check-库存充足校验
            let stockNum = parseInt($('#stockNum').text());
            let num = parseInt($("#rp_transfersubbillsForm-num").val());
            if (num > stockNum) {
                alert('当前物料在该仓库存不足,无法保存,请选择其他仓库或补充物料后重试!')
                return
            }

            //check-重复产品校验
            for (const n of subTableData) {
                let productId = $('#rp_transfersubbillsForm-productId option:selected').val();
                if (n.productId === productId) {
                    alert('已经添加该产品,可直接修改数量,不能重复添加!')
                    return
                }
            }



            //do-保存产品信息
            let transferBillId = $("#rp_transferbillsForm-transferBillId").text();
            let rowNum = $("#rp_transfersubbills tbody tr").size();
            // console.log('rowNum:' + rowNum);
            let productData = getFormData({
                formId: 'rp_transfersubbillsForm'
            })

            console.log('productData:' + JSON.stringify(productData.data))
            let rowData = formDataToRowData(productData.data)
            rowData.transferBillId = transferBillId;
            // console.log('rowData:' + JSON.stringify(rowData))
            //检查是否存在序号,区分新增还是编辑
            if (rowData.rowId) {
                for (let n in subTableData) {
                    //此处不用===避免,字符类型不匹配导致相关问题
                    if (subTableData[n].rowId == rowData.rowId) {
                        subTableData[n] = rowData;
                        break;
                    }
                }
            } else {
                rowData.rowId = rowNum + 1;
                subTableData.push(rowData);
            }

            //return-重新加载子表
            loadDataSubTable({
                elementId: 'rp_transfersubbills',
                dataArray: subTableData
            });

        })

        // 删除产品按钮
        $('#btnDeleteProduct').click(function () {

            //check-检查选中
            let subTable = $('#rp_transfersubbills').DataTable();
            let rowSelect = subTable.row('.selected').data();

            if (!rowSelect) {
                alert('请先点击表格选中需要删除的产品!')
                return
            }
            //check-检查状态

            if (rowSelect.status !== '新增') {
                alert('非新增状态不允许删除!')
                return
            }

            //do- 删除元素
            for (const n in subTableData) {
                if (subTableData[n].rowId === rowSelect.rowId) {
                    subTableData.splice(n, 1);
                }
            }
            //重新排序rowId,避免空缺
            for (const n in subTableData) {
                subTableData[n].rowId = parseInt(n) + 1;
            }

            //return-重新加载子表
            loadDataSubTable({
                elementId: 'rp_transfersubbills',
                dataArray: subTableData
            });
            //return-自动点击新增按钮,防止遗留数据产生错误
            $('#btnAddProduct').click()


        })



        //表单打印按钮
        $('#rp_transferbillsPrint').click(function () {


            if (!mainTableData.transferBillId) {
                alert('请先选中需要打印的单据!')
                return
            }


            fillTableInputs({
                tableId: 'printViewMainTable',
                params: mainTableData
            })


            fillTableRows({
                tableId: 'printViewSubTable',
                cols: ['rowId', 'productId', 'productName', 'unit', 'num'],
                data: subTableData
            })

            $('#printViewModal').modal('show')
        })

        //确认打印按钮
        $('#confirmPrintBtn').click(function () {
            Print('#printViewDiv', {
                onStart: function () {
                    console.log('onStart', new Date())
                },
                onEnd: function () {
                    console.log('onEnd', new Date())
                }
            })
        })


        //打印关闭按钮
        $('#printCloseBtn').click(function () {
            $('#printViewModal').modal('hide')
        })

        //表单保存按钮
        $('#rp_transferbillsSave').click(function () {

            //check-检查单据状态
            if (mainTableData.status === '已审核') {
                alert('已审核单据无法保存')
                return
            }

            //check-检查必填项val
            if (!($('#rp_transferbillsForm-inWarehouseId option:selected').val() &&
                    $('#rp_transferbillsForm-outWarehouseId option:selected').val())) {
                alert('拨出仓库,拨入仓库未选,请检查!')
                return;
            }

            //check-仓库不同校验
            if ($('#rp_transferbillsForm-inWarehouseId option:selected').val() ===
                $('#rp_transferbillsForm-outWarehouseId option:selected').val()) {
                alert('拨出仓库与拨入仓库必须为不同仓库!')
                return
            }

            //check-检查子表项目
            if (subTableData.length === 0) {
                alert('未添加需要调拨的产品,无法保存单据,请检查!')
                return;
            }

            //do-保存表单
            saveTransferBill();
        })

        //批量导出按钮
        $('#batchExportBtn').click(async function () {
            $('#exportModal').modal('show')
        })

        //执行批量导出按钮
        $('#doExportBtn').click(async function () {

            let titleTextArr = []
            let titleValueArr = []

            //主表字段选择
            $('#exportModelForm').find('input').each(function () {
                // console.log("this:" + JSON.stringify(this));
                if ($(this).is(':checked')) {

                    let title = $(this).next('label').text();
                    titleTextArr.push(title)
                    let value = $(this).val();
                    titleValueArr.push(value)
                }
            })

            //子表字段选择
            $('#exportSubModelForm').find('input').each(function () {
                // console.log("this:" + JSON.stringify(this));
                if ($(this).is(':checked')) {
                    let title = $(this).next('label').text();
                    titleTextArr.push(title)
                    let value = $(this).val();
                    titleValueArr.push(value)
                }
            })



            exportData = await getDataBySql({
                sql: 'transferBillsExportSql',
                params: {
                    filter: mainTableFilter
                }
            })
            console.log('exportData', exportData);

            let tableArr = []

            tableArr.push(titleTextArr)

            if (exportData.length > 0) {
                for (let i = 0; i < exportData.length; i++) {
                    let row = []
                    for (const p in exportData[i]) {
                        if (titleValueArr.includes(p)) {
                            row.push(exportData[i][p])
                        }
                    }
                    tableArr.push(row)
                }
            }

            console.log('tableArr', tableArr);



            var sheet = XLSX.utils.aoa_to_sheet(tableArr);
            openDownloadDialog(sheet2blob(
                sheet), '调拨单.xlsx');
        })

        //批量导入按钮
        $('#batchImportBtn').click(function () {
            selectFile()
        })

        //数据模板按钮
        $('#importModelBtn').click(function () {
            var aoa = [
                [
                    '调拨单号',
                    '调拨日期',
                    '拨出仓库编号',
                    '拨出仓库',
                    '拨入仓库编号',
                    '拨入仓库',
                    '经手人',
                    '制单人',
                    '审核人',
                    '单据备注',
                    '行号',
                    '产品编号',
                    '产品名称',
                    '单位',
                    '数量',
                    '子表备注'
                ],
            ];
            var sheet = XLSX.utils.aoa_to_sheet(aoa);
            openDownloadDialog(sheet2blob(sheet), '调拨单模板.xlsx');
        })

        //初始化过滤器日期组
        loadDatePickers({
            elementIds: [
                // 'filterForm-borrowDateB',
                // 'filterForm-borrowDateE',
                'filterForm-makeDateB',
                'filterForm-makeDateE'
            ]
        })


        //初始化组件
        loadDatePicker({
            elementId: "rp_transferbillsForm-transferDate",
        })




        //初始化selector

        loadBootStrapSelector({
            elementId: 'rp_transferbillsForm-customerShortName',
            sqlParams: {
                sql: 'getCustomerSelector'
            },
        })






        loadBootStrapSelector({
            elementId: 'rp_transferbillsForm-operator',
            sqlParams: {
                sql: 'getStaffSelector'
            },
            // initValue:''
        })

        loadBootStrapSelector({
            elementId: 'rp_transferbillsForm-auditor',
            sqlParams: {
                sql: 'getStaffSelector'
            },
            // initValue:''
        })


        loadBootStrapSelector({
            elementId: 'rp_transfersubbillsForm-productId',
            sqlParams: {
                sql: 'getStoreProductSelector',
                params: {
                    filter: 'warehouseId ="W1"'
                }
            },
        })



        loadBootStrapSelector({
            elementId: 'rp_transferbillsForm-customerBelongShort',
            // initValue:'',
            sqlParams: {
                sql: 'getCustomerSelector',
                params: {
                    filter: 'customerType="客户"'
                }
            },
            // initValue:'中国,北京'
        })


        loadBootStrapSelector({
            elementId: 'rp_transferbillsForm-sendWay',
            sqlParams: {
                sql: 'getOptionSelector',
                params: {
                    filter: 'optionClass="出货方式"',
                    orderBy: 'orderId'
                }
            },
        })


        loadBootStrapSelector({
            elementId: 'rp_transferbillsForm-outWarehouseId',
            sqlParams: {
                sql: 'getWarehouseSelector',
                params: {
                    filter: 'warehouseId<>"W3"'
                }
            },
            // initValue: 'W1'
        })

        loadBootStrapSelector({
            elementId: 'rp_transferbillsForm-inWarehouseId',
            sqlParams: {
                sql: 'getWarehouseSelector',
            },
            // initValue: 'W1'
        })






        //选定产品修改事件
        addProductListener();
        //仓库选择修改事件
        addOutWarehouseListener();

        //添加文件监听事件
        addFileReaderListener()



        // 关闭事件重置选中datatable选中,避免显示错误
        $('#rp_transferbillsModal').on('hidden.bs.modal', function () {
            let table = $('#rp_transferbills').DataTable();
            table.$('tr.selected').removeClass('selected');
            mainTableData = {}

            // //添加监听出来扫描modal关闭 表单modal失效问题
            $(this).removeData('bs.modal');
            $(document.body).addClass("modal-open");
        })

        //自动调整modal显示位置
        $('#scanModal').on('show.bs.modal', function (
            e) {
            // 关键代码，如没将modal设置为 block，则$modala_dialog.height() 为零 
            $(this).css('display', 'block');
            // var modalHeight = $(window).height() / 2;
            let modalHeight = $(window).height() / 3;
            $(this).find('.modal-dialog').css({
                'margin-top': modalHeight
            });
        });






    });
</script>