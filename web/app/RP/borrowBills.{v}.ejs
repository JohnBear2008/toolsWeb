<!-- 导入css库 -->
<link rel="stylesheet" type="text/css" href="/rpLib/css/divCss.css">

<!-- 导入js库 -->
<script type="text/javascript" src="/rpLib/publicFuns.js"></script>
<script type="text/javascript" src="/rpLib/addonFuns.js"></script>

<!-- 导入datatables -->
<link rel="stylesheet" type="text/css" href="/rpLib/DataTables/datatables.min.css" />
<script type="text/javascript" src="/rpLib/DataTables/datatables.min.js"></script>

<!-- 导入bootstrapSelect -->
<link rel="stylesheet" href="/rpLib/bootstrapSelect/css/bootstrap-select.min.css" />
<script type="text/javascript" src="/rpLib/bootstrapSelect/js/bootstrap-select.min.js"></script>


<!-- 导入bootstrapSwitch -->
<link rel="stylesheet" href="/rpLib/bootstrapSwitch/css/bootstrap3/bootstrap-switch.min.css" />
<script type="text/javascript" src="/rpLib/bootstrapSwitch/js/bootstrap-switch.min.js"></script>

<!-- 导入datatimepicker -->
<link href="/rpLib/datetimepicker/bootstrap-datetimepicker.min.css" rel="stylesheet" media="screen">
<script type="text/javascript" src="/rpLib/datetimepicker/bootstrap-datetimepicker.min.js" charset="UTF-8"></script>
<script type="text/javascript" src="/rpLib/datetimepicker/bootstrap-datetimepicker.zh-CN.js" charset="UTF-8"></script>


<!-- 导入qrcode 二维码库 -->
<script type="text/javascript" src="/rpLib/qrcode/qrcode.min.js"></script>

<!-- 导入instascan 二维码扫描库 -->
<script type="text/javascript" src="/rpLib/instascan/instascan.min.js"></script>

<!-- 导入print 打印库 -->
<script type="text/javascript" src="/rpLib/print/print.js"></script>

<!-- 导入js-xlsx -->
<script type="text/javascript" src="/rpLib/sheetJs/xlsx.core.min.js"></script>

<!-- 加载foil外框架 -->
<% layout(locals.global.theme_layout_dir+'/master.ejs') -%>

<!-- 定义html元素 -->
<div class="rpContent">

    <!-- 按钮：用于打开模态框 -->

    <!-- <div class="row clearfix">
        <div class="col-md-12 column">
            <div class="page-header">
                <h1>库存管理-借货单</h1>
            </div>
        </div>
    </div> -->


    <div class="row clearfix">
        <div class="col-md-12 column">


            <!-- 定义基础模块数据表 -->

            <div class="panel panel-default" id='mainTablePanel'>
                <div class="panel-heading">
                    <div style="float: left;">
                        <div class="btn-group">
                            <button type="button" class="btn btn-primary">过滤</button>
                            <button type="button" id="btnFilterToday" class="btn btn-default">当天</button>
                            <button type="button" id="btnFilterUnAudit" class="btn btn-default">待审核</button>
                            <button type="button" id="btnFilterDiy" class="btn btn-default">自定义</button>
                        </div>
                    </div>

                    <!-- excel -->
                    <div style="float: left; margin-left: 15px;">
                        <div class="btn-group">
                            <button type="button" class="btn btn-primary">excel</button>
                            <button type="button" id="batchImportBtn" class="btn btn-default">批量导入</button>
                            <button type="button" id="batchExportBtn" class="btn btn-default">批量导出</button>
                            <button type="button" id="importModelBtn" class="btn btn-default">数据模板</button>
                            <input type="file" id="file" style="display:none;"
                                accept="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet">
                        </div>
                    </div>
                    <!-- 功能组按钮 -->
                    <div style="float:right">

                        <button id='btnBorrowBillNew' type="button" class="btn btn-info">新增</button>
                        <!-- <button id='btnBorrowBillDelete' type="button" class="btn btn-danger">删除</button> -->
                        <button id='btnBorrowBillAudit' type="button" class="btn btn-success">审核</button>
                        <button id='btnBorrowBillReject' type="button" class="btn btn-warning">驳回</button>
                        <!-- <button type="button" class="btn btn-info" id="listPrintBtn">
                            打印借货码
                        </button> -->
                        <button type="button" class="btn btn-primary" id="rp_borrowbillsPrint">
                            打印借货单
                        </button>
                    </div>
                    <div style="clear:both"></div>
                </div>
                <div class="panel-body">
                    <table id="rp_borrowbills" class=" table table-striped table-bordered" style="width:100%">
                        <thead>
                            <tr>
                                <th>借货单号</th>
                                <th>借货日期</th>
                                <th>客户简称</th>
                                <th>联系人</th>
                                <th>电话</th>
                                <th width='300'>地址</th>
                                <th>客户归属</th>
                                <th>出货方式</th>
                                <th>审核人</th>
                                <th>备注</th>
                                <th>状态</th>
                                <th>搜索值</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>

            <!-- 定义主表关联附表数据表 -->

            <div class="panel panel-default" id='subTablePanel' style="width:100%;display: none;">
                <div class="panel-heading">
                    <h3 class="panel-title">产品清单</h3>
                </div>
                <div class="panel-body">
                    <table id="rp_borrowbillsSub" class=" table table-striped table-bordered" style="min-width:100%;">
                        <thead>
                            <tr>
                                <th>序号</th>
                                <th>产品编号</th>
                                <th>产品描述</th>
                                <th>单位</th>
                                <th>数量</th>
                                <th>已还</th>
                                <th>未还</th>
                                <th>备注</th>
                                <th>状态</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- 过滤器模态框（Modal） -->
    <div class="modal fade" id="filterModal" tabindex="-1" role="dialog" aria-hidden="true">

        <div class="modal-dialog" style="width: 800px;">

            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
                        &times;
                    </button>
                    <h4 class="modal-title">
                        过滤器
                    </h4>
                </div>
                <div class="modal-body">

                    <div class="panel panel-info">
                        <div class="panel-body" style="height:300px">
                            <form class="form-horizontal" role="form" id='filterForm'>

                                <div class="form-group">
                                    <label class="col-sm-2 control-label">借货日期</label>
                                    <div class="col-sm-5">
                                        <div class="input-group date form_date ">
                                            <input id="filterForm-borrowDateB" class="form-control " size="16"
                                                type="text" value="" placeholder="起始日期" readonly>
                                            <span class="input-group-addon"><span
                                                    class="glyphicon glyphicon-calendar"></span></span>
                                        </div>
                                    </div>

                                    <div class="col-sm-5">
                                        <div class="input-group date form_date ">
                                            <input id="filterForm-borrowDateE" class="form-control " size="16"
                                                type="text" value="" placeholder="结束日期" readonly>
                                            <span class="input-group-addon"><span
                                                    class="glyphicon glyphicon-calendar"></span></span>
                                        </div>
                                    </div>

                                </div>

                                <div class="form-group">
                                    <label class="col-sm-2 control-label">制单日期</label>
                                    <div class="col-sm-5">
                                        <div class="input-group date form_date ">
                                            <input id="filterForm-makeDateB" class="form-control " size="16" type="text"
                                                value="" placeholder="起始日期" readonly>
                                            <span class="input-group-addon"><span
                                                    class="glyphicon glyphicon-calendar"></span></span>
                                        </div>
                                    </div>

                                    <div class="col-sm-5">
                                        <div class="input-group date form_date ">
                                            <input id="filterForm-makeDateE" class="form-control " size="16" type="text"
                                                value="" placeholder="结束日期" readonly>
                                            <span class="input-group-addon"><span
                                                    class="glyphicon glyphicon-calendar"></span></span>
                                        </div>
                                    </div>

                                </div>



                                <div class="form-group">
                                    <label class="col-sm-2 control-label"></label>

                                    <div class="col-sm-10">
                                        <button type="button" class="btn btn-default" data-dismiss="modal"
                                            id="closeFilter">关闭
                                        </button>
                                        <button type="button" class="btn btn-primary" id="confirmFilter">
                                            确定
                                        </button>

                                    </div>


                                </div>
                            </form>

                        </div>
                    </div>


                </div><!-- /.modal-content -->
            </div><!-- /.modal -->
        </div>
    </div>

    <!-- 导出模态框（Modal） -->
    <div class="modal fade" id="exportModal" tabindex="-1" role="dialog" aria-hidden="true">

        <div class="modal-dialog" style="width: 800px;">

            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
                        &times;
                    </button>
                    <h4 class="modal-title">
                        字段选择
                    </h4>
                </div>
                <div class="modal-body">

                    <div class="panel panel-info">
                        <div class="panel-body" style="height:600px">

                            <label class="col-sm-2 control-label">主表字段</label>
                            <div class="col-sm-4">

                                <div class="panel panel-default">
                                    <div class="panel-body" style="height:500px">

                                        <form class="form-horizontal" role="form" id='exportModelForm'>

                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="billFrom"
                                                    checked>
                                                <label class="form-check-label">单据来源</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="borrowBillId"
                                                    checked>
                                                <label class="form-check-label">借货单号</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="customerId"
                                                    checked>
                                                <label class="form-check-label">客户编号</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input"
                                                    value="customerShortName" checked>
                                                <label class="form-check-label">客户简称</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="customerName"
                                                    checked>
                                                <label class="form-check-label">客户名称</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input"
                                                    value="customerBelongShort" checked>
                                                <label class="form-check-label">客户归属</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="contact" checked>
                                                <label class="form-check-label">联系人</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="mobilePhone"
                                                    checked>
                                                <label class="form-check-label">移动电话</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="workPhone"
                                                    checked>
                                                <label class="form-check-label">工作电话</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="operator"
                                                    checked>
                                                <label class="form-check-label">经手人</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="maker" checked>
                                                <label class="form-check-label">制单人</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="auditor" checked>
                                                <label class="form-check-label">审核人</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="makeDate"
                                                    checked>
                                                <label class="form-check-label">制单日期</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="auditDate"
                                                    checked>
                                                <label class="form-check-label">审核日期</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="status" checked>
                                                <label class="form-check-label">单据状态</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="remark" checked>
                                                <label class="form-check-label">备注</label>
                                            </div>

                                        </form>
                                    </div>
                                </div>

                            </div>

                            <label class="col-sm-2 control-label">子表字段</label>
                            <div class="col-sm-4">

                                <div class="panel panel-default">
                                    <div class="panel-body">
                                        <form class="form-horizontal" role="form" id='exportSubModelForm'>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="rowId" checked>
                                                <label class="form-check-label">序号</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="productId"
                                                    checked>
                                                <label class="form-check-label">产品编号</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="productName"
                                                    checked>
                                                <label class="form-check-label">产品名称</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="warehouseId"
                                                    checked>
                                                <label class="form-check-label">仓库编号</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="warehouseName"
                                                    checked>
                                                <label class="form-check-label">仓库名称</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="unit" checked>
                                                <label class="form-check-label">单位</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="num" checked>
                                                <label class="form-check-label">数量</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="returnNum"
                                                    checked>
                                                <label class="form-check-label">已还</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="unReturnNum"
                                                    checked>
                                                <label class="form-check-label">未还</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="subStatus"
                                                    checked>
                                                <label class="form-check-label">借货状态</label>
                                            </div>
                                            <div class="form-check">
                                                <input type="checkbox" class="form-check-input" value="subRemark"
                                                    checked>
                                                <label class="form-check-label">子表备注</label>
                                            </div>


                                        </form>
                                    </div>
                                </div>

                            </div>




                            <div class="col-sm-12">
                                <button type="button" class="btn btn-primary" id="doExportBtn">
                                    确认导出
                                </button>
                            </div>

                        </div>
                    </div>


                </div><!-- /.modal-content -->
            </div><!-- /.modal -->
        </div>
    </div>

    <!-- 打印预览模态框（Modal） -->
    <div class="modal fade" id="printViewModal">
        <div class="modal-dialog" style="width: 80%;">
            <div class="modal-content">
                <div class="modal-body">
                    <div id="printViewDiv">

                        <h2>弘讯科技股份有限公司借货单</h2>
                        <hr>

                        <table id='printViewMainTable' style="border: 0ch; width: 100%;table-layout:fixed;">
                            <tbody>
                                <tr>
                                    <td width='80px'>借货单号:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-borrowBillId"></p>
                                    </td>
                                    <td width='80px'>客户编号:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-customerId"></p>
                                    </td>
                                </tr>
                                <tr>
                                    <td width='80px'>客户简称:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-customerShortName">
                                        </p>
                                    </td>
                                    <td width='80px'>客户名称:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-customerName"></p>
                                    </td>
                                </tr>
                                <tr>
                                    <td width='80px'>联系人:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-contact"></p>
                                    </td>
                                    <td width='80px'>移动电话:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-mobilePhone"></p>
                                    </td>
                                </tr>
                                <tr>
                                    <td width='80px'>出货方式:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-sendWay"></p>
                                    </td>
                                    <td width='80px'>经手人:</td>
                                    <td>
                                        <p class="form-control-static" id="printViewMainTable-operator"></p>
                                    </td>
                                </tr>

                            </tbody>
                        </table>



                        <hr>
                        <table id='printViewSubTable' class="table table-condensed">

                            <thead>
                                <tr>
                                    <th>序号</th>
                                    <th>产品编号</th>
                                    <th>产品名称</th>
                                    <th>单位</th>
                                    <th>数量</th>
                                </tr>
                            </thead>
                            <tbody>

                            </tbody>
                        </table>

                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-default" id="printCloseBtn">关闭
                    </button>
                    <button type="button" class="btn btn-primary" id="confirmPrintBtn">
                        确认打印
                    </button>
                </div>
            </div>
        </div>
    </div>



    <!-- 打印预览模态框（Modal） -->
    <!-- <div class="modal fade" id="listPrintViewModal">
        <div class="modal-dialog" style="width: 600px;">
            <div class="modal-content">
                <div class="modal-body">

                    <h2>弘讯科技股份有限公司借货清单</h2>
                    <hr>

                    <div id="listPrintViewDiv">


                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-default" id="listPrintCloseBtn">关闭
                    </button>
                    <button type="button" class="btn btn-primary" id="listPrintConfirmBtn">
                        确认打印
                    </button>
                </div>
            </div>
        </div>
    </div> -->





    <!-- 模态框（Modal） -->
    <div class="modal fade" id="rp_borrowbillsModal" tabindex="-1" role="dialog" aria-hidden="true"
        data-backdrop="static">

        <div class="modal-dialog" style="width: 75%;">

            <!-- 按钮触发模态框 -->
            <button class="btn btn-primary" data-toggle="modal" data-target="#rp_borrowbillsModal"
                id="rp_borrowbillsModalOpen" style="display: none;">
                开始演示模态框
            </button>
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
                        &times;
                    </button>
                    <h4 class="modal-title">
                        表单详情
                    </h4>


                </div>
                <div class="modal-body">


                    <!-- 表单面板 -->
                    <div class="panel panel-default">
                        <div class="panel-body">


                            <form class="form-horizontal" role="form" id='rp_borrowbillsForm'>

                                <!-- <input id="rp_borrowbillsForm-DBID" type="hidden" value=""> -->

                                <div class="form-group">
                                    <label class="col-sm-1 control-label"
                                        for="rp_borrowbillsForm-borrowBillId">借货单号</label>
                                    <div class="col-sm-5">
                                        <p class="form-control-static" id="rp_borrowbillsForm-borrowBillId"></p>
                                    </div>



                                    <label class="col-sm-1 control-label"
                                        for="rp_borrowbillsForm-borrowDate">借货日期:</label>
                                    <div class="col-sm-5 ">
                                        <div class="input-group date form_date ">
                                            <input id="rp_borrowbillsForm-borrowDate" class="form-control " size="16"
                                                type="text" value="" readonly>
                                            <span class="input-group-addon"><span
                                                    class="glyphicon glyphicon-calendar"></span></span>
                                        </div>
                                    </div>
                                </div>




                                <div class="form-group">
                                    <label class="col-sm-1 control-label"
                                        for="rp_borrowbillsForm-fromBillId">单据来源</label>
                                    <div class="col-sm-5">
                                        <p class="form-control-static" id="rp_borrowbillsForm-billFrom"></p>
                                    </div>


                                    <label class="col-sm-1 control-label "
                                        for="rp_borrowbillsForm-customerId">客户编号</label>
                                    <div class="col-sm-5">
                                        <p class="form-control-static" id="rp_borrowbillsForm-customerId"></p>
                                    </div>

                                </div>

                                <div class="form-group">

                                    <label class="col-sm-1 control-label text-warning"
                                        for="rp_borrowbillsForm-customerShortName">客户简称</label>
                                    <div class="col-sm-5">
                                        <select id="rp_borrowbillsForm-customerShortName"
                                            class="selectpicker form-control" data-live-search="true" data-size="10">
                                        </select>
                                    </div>





                                    <label class="col-sm-1 control-label"
                                        for="rp_borrowbillsForm-customerName">客户名称</label>
                                    <div class="col-sm-5">
                                        <input class="form-control" id="rp_borrowbillsForm-customerName" type="text">
                                    </div>


                                </div>

                                <div class="form-group">
                                    <label class="col-sm-1 control-label" for="rp_borrowbillsForm-isInland">国内外</label>
                                    <div class="col-sm-5">
                                        <select id="rp_borrowbillsForm-isInland" class="selectpicker">

                                        </select>
                                    </div>

                                    <!-- <label class="col-sm-1 control-label"
                                        for="rp_borrowbillsForm-customerArea">客户区域</label>
                                    <div class="col-sm-5">
                                        <select id="rp_borrowbillsForm-customerArea" class="selectpicker form-control"
                                            data-live-search="true" data-size="10">
                                        </select>
                                    </div> -->
                                </div>

                                <div class="form-group">
                                    <label class="col-sm-1 control-label text-warning"
                                        for="customerContacts">可选联系人</label>
                                    <div class="col-sm-5">
                                        <!-- <input class="form-control" id="rp_borrowbillsForm-contact" type="text"> -->
                                        <select id="customerContacts" class="selectpicker">
                                        </select>
                                    </div>

                                    <!-- <label class="col-sm-1 control-label"
                                        for="rp_borrowbillsForm-customerId">保固期</label>
                                    <div class="col-sm-5">
                                        <p class="form-control-static" id="rp_borrowbillsForm-warrantyPeriod"></p>
                                    </div> -->
                                </div>

                                <div class="form-group">
                                    <label class="col-sm-1 control-label text-warning"
                                        for="rp_borrowbillsForm-contact">联系人</label>
                                    <div class="col-sm-5">
                                        <input class="form-control" id="rp_borrowbillsForm-contact" type="text">
                                    </div>

                                    <label class="col-sm-1 control-label text-warning"
                                        for="rp_borrowbillsForm-mobilePhone">移动电话</label>
                                    <div class="col-sm-5">
                                        <input class="form-control" id="rp_borrowbillsForm-mobilePhone" type="text">
                                    </div>
                                </div>

                                <div class="form-group">
                                    <label class="col-sm-1 control-label text-warning"></label>
                                    <div class="col-sm-5">
                                        <button type="button" class="btn btn-warning" id="addContactsBtn">添加到可选联系人
                                        </button>
                                    </div>
                                </div>

                                <div class="form-group">
                                    <label class="col-sm-1 control-label text-warning"
                                        for="rp_borrowbillsForm-customerBelongShort">归属公司简称</label>
                                    <div class="col-sm-5">
                                        <!-- <input class="form-control" id="rp_borrowbillsForm-customerBelongShort"
                                            type="text"> -->
                                        <select id="rp_borrowbillsForm-customerBelongShort"
                                            class="selectpicker form-control" data-live-search="true" data-size="10">
                                        </select>
                                    </div>

                                    <label class="col-sm-1 control-label text-warning"
                                        for="rp_borrowbillsForm-sendWay">出货方式</label>
                                    <div class="col-sm-5">
                                        <select id="rp_borrowbillsForm-sendWay" class="selectpicker form-control">
                                        </select>
                                    </div>
                                </div>

                                <div class="form-group">

                                    <label class="col-sm-1 control-label">快递类型</label>
                                    <div class="col-sm-5">
                                        <select id="rp_borrowbillsForm-expressType" class="selectpicker form-control"
                                            data-live-search="true" data-size="5" data-dropup-auto="false">
                                        </select>
                                    </div>

                                    <label class="col-sm-1 control-label">快递单号</label>
                                    <div class="col-sm-5">
                                        <input id="rp_borrowbillsForm-expressId" class="form-control " type="text">
                                    </div>

                                </div>

                                <div class="form-group">
                                    <label class="col-sm-1 control-label text-warning"
                                        for="rp_borrowbillsForm-operator">经手人</label>
                                    <div class="col-sm-5">
                                        <!-- <input class="form-control" id="rp_borrowbillsForm-operator" type="text"> -->
                                        <select id="rp_borrowbillsForm-operator" class="selectpicker"
                                            data-live-search="true" data-size="5">
                                        </select>
                                    </div>

                                    <label class="col-sm-1 control-label" for="rp_borrowbillsForm-auditor">审核人</label>
                                    <div class="col-sm-5">
                                        <select id="rp_borrowbillsForm-auditor" class="selectpicker"
                                            data-live-search="true" data-size="5">
                                        </select>
                                    </div>
                                </div>

                                <div class="form-group">
                                    <label class="col-sm-1 control-label text-warning"
                                        for="rp_borrowbillsForm-address">地址</label>
                                    <div class="col-sm-11">
                                        <input class="form-control" id="rp_borrowbillsForm-address" type="text">
                                    </div>
                                </div>

                                <div class="form-group">
                                    <label class="col-sm-1 control-label">单据备注</label>
                                    <div class="col-sm-11">
                                        <input class="form-control" id="rp_borrowbillsForm-remark" type="text">
                                    </div>
                                </div>

                                <!-- <div class="form-group">
                                    <label class="col-sm-1 control-label" for="msgSwitch">短信提醒</label>
                                    <div class="col-sm-11">
                                        <input id="msgSwitch" type="checkbox">
                                    </div>
                                </div> -->


                                <div class="form-group">
                                    <label class="col-sm-1 control-label" for="rp_borrowbillsForm-maker">制单人</label>
                                    <div class="col-sm-5">
                                        <p class="form-control-static" id="rp_borrowbillsForm-maker"></p>
                                    </div>

                                    <label class="col-sm-1 control-label" for="rp_borrowbillsForm-makeDate">制单日期</label>
                                    <div class="col-sm-5">
                                        <p class="form-control-static" id="rp_borrowbillsForm-makeDate"></p>
                                    </div>
                                </div>

                                <div class="form-group">

                                    <label class="col-sm-1 control-label">单据状态</label>
                                    <div class="col-sm-5">
                                        <p class="form-control-static" id="rp_borrowbillsForm-status"></p>
                                    </div>


                                    <label class="col-sm-1 control-label"
                                        for="rp_borrowbillsForm-auditDate">审核日期</label>
                                    <div class="col-sm-5">
                                        <p class="form-control-static" id="rp_borrowbillsForm-auditDate"></p>
                                    </div>
                                </div>

                            </form>

                            <!-- 子表 -->
                            <hr>

                            <div class="row clearfix">
                                <div class="col-md-12 column">

                                    <!-- 功能组按钮 -->
                                    <div class="well">
                                        <button id='btnAddProduct' type="button" class="btn btn-info">新增产品</button>
                                        <button id='btnDeleteProduct' type="button" class="btn btn-danger">删除产品</button>
                                        <!-- <button id='btnScan' type="button" class="btn btn-success">扫描新增</button> -->

                                    </div>

                                    <!-- 定义基础模块数据表 -->
                                    <table id="rp_borrowsubbills" class=" table table-striped table-bordered"
                                        style="width:100%">
                                        <thead>
                                            <tr>
                                                <th>序号</th>
                                                <th>产品编号</th>
                                                <th>产品描述</th>
                                                <th>单位</th>
                                                <th>数量</th>
                                                <th>已还</th>
                                                <th>未还</th>
                                                <th>备注</th>
                                                <th>状态</th>
                                            </tr>
                                        </thead>
                                        <tbody></tbody>
                                    </table>

                                    <div class="panel panel-default" id='rp_borrowsubbillsPanel'>
                                        <div class="panel-heading">
                                            <h3 class="panel-title">产品详情</h3>
                                        </div>
                                        <div class="panel-body">




                                            <form class="form-horizontal" role="form" id='rp_borrowsubbillsForm'>

                                                <!-- <input id="rp_borrowsubbillsForm-DBID" type="hidden" value=""> -->
                                                <input id="rp_borrowsubbillsForm-borrowBillId" type="hidden" value="">

                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label">序号</label>
                                                    <div class="col-sm-3">
                                                        <p class="form-control-static" id="rp_borrowsubbillsForm-rowId">
                                                        </p>
                                                    </div>

                                                    <label class="col-sm-1 control-label">可用库存</label>
                                                    <div class="col-sm-3">
                                                        <p class="form-control-static" id="stockNum">
                                                        </p>
                                                    </div>

                                                    <label class="col-sm-1 control-label">状态</label>
                                                    <div class="col-sm-3">
                                                        <p class="form-control-static"
                                                            id="rp_borrowsubbillsForm-status">
                                                        </p>
                                                    </div>


                                                </div>


                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label">产品编号</label>
                                                    <div class="col-sm-5">
                                                        <select id="rp_borrowsubbillsForm-productId"
                                                            class="selectpicker form-control" data-live-search="true"
                                                            data-size="5" data-dropup-auto="false">
                                                        </select>
                                                    </div>

                                                    <label class="col-sm-1 control-label">仓库</label>
                                                    <div class="col-sm-5">
                                                        <select id="rp_borrowsubbillsForm-warehouseId"
                                                            class="selectpicker form-control" data-live-search="true"
                                                            data-size="5" data-dropup-auto="false">
                                                        </select>
                                                    </div>



                                                </div>

                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label">产品名称</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static"
                                                            id="rp_borrowsubbillsForm-productName">
                                                        </p>
                                                    </div>

                                                    <label class="col-sm-1 control-label">产品描述</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static"
                                                            id="rp_borrowsubbillsForm-productDescription">
                                                        </p>
                                                    </div>
                                                </div>

                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label">产品类别</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static"
                                                            id="rp_borrowsubbillsForm-productClass">
                                                        </p>

                                                    </div>

                                                    <label class="col-sm-1 control-label">系统分类</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static"
                                                            id="rp_borrowsubbillsForm-systemType">
                                                        </p>
                                                    </div>

                                                </div>

                                                <div class="form-group">

                                                    <label class="col-sm-1 control-label">出厂编号</label>
                                                    <div class="col-sm-5">
                                                        <input class="form-control" id="rp_borrowsubbillsForm-factoryNo"
                                                            type="text">
                                                    </div>

                                                    <label class="col-sm-1 control-label">生产日期</label>
                                                    <div class="col-sm-5">
                                                        <input id="rp_borrowsubbillsForm-productDate"
                                                            class="form-control " type="text" value="" readonly>
                                                    </div>


                                                </div>



                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label">单位</label>
                                                    <div class="col-sm-5">
                                                        <p class="form-control-static" id="rp_borrowsubbillsForm-unit">
                                                        </p>

                                                    </div>

                                                    <label class="col-sm-1 control-label">数量</label>
                                                    <div class="col-sm-5">
                                                        <input class="form-control" id="rp_borrowsubbillsForm-num"
                                                            type="text">
                                                    </div>

                                                </div>


                                                <div class="form-group">

                                                    <label class="col-sm-1 control-label">备注</label>
                                                    <div class="col-sm-11">
                                                        <input class="form-control" id="rp_borrowsubbillsForm-remark"
                                                            type="text">
                                                    </div>
                                                </div>

                                                <div class="form-group">
                                                    <label class="col-sm-1 control-label"></label>
                                                    <div class="col-sm-5">
                                                        <button type="button" class="btn btn-primary"
                                                            id="btnProductSave">
                                                            保存
                                                        </button>

                                                    </div>
                                                </div>

                                            </form>


                                        </div>
                                    </div>

                                </div>
                            </div>

                            <!-- 扫描模态框（Modal） -->

                            <!-- <div class="modal fade" id="scanModal">
                                <div class="modal-dialog" style="width: 675px;">
                                    <div class="modal-content">
                                        <div class="modal-body">请将产品二维码放置于摄像头内
                                            <div><button id='btnScanStop' type="button"
                                                    class="btn btn-info">停止扫描</button></div>
                                            <div style="width:640;height:480px"><video id="preview"></video>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div> -->


                        </div>
                    </div>

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal" id="rp_borrowbillsModalClose">关闭
                    </button>

                    <button type="button" class="btn btn-primary" id="rp_borrowbillsSave">
                        保存单据
                    </button>
                </div>
            </div><!-- /.modal-content -->
        </div><!-- /.modal -->
    </div>



</div>



<script type="text/javascript">
    //申请表主表dataTable column 数组

    const mainColumnsArray = [{
        data: 'borrowBillId',
        // visible: 'false'
    }, {
        data: 'borrowDate'
    }, {
        data: 'customerShortName'
    }, {
        data: 'contact',
    }, {
        data: 'mobilePhone',
    }, {
        data: 'address',
    }, {
        data: 'customerBelongShort',
    }, {
        data: 'sendWay',
    }, {
        data: 'auditor',
    }, {
        data: 'remark',
    }, {
        data: 'status',
    }, {
        data: 'searchText',
        visible: false
    }];

    //申请表子表dataTable column 数组
    const subColumnsArray = [{
        data: 'rowId',
    }, {
        data: 'productId',
    }, {
        data: 'productDescription',
    }, {
        data: 'unit',
    }, {
        data: 'num',
    }, {
        data: 'returnNum',
    }, {
        data: 'unReturnNum',
    }, {
        data: 'remark',
    }, {
        data: 'status',
    }]



    //选中客户后,填写其他相关信息

    const fillCustomerInfo = async (customerShortName) => {
        let r1 = await getDataBySql({
            sql: 'getCustomers',
            params: {
                filter: 'customerShortName="' + customerShortName + '"'
            }
        })

        // console.log('r1:' + JSON.stringify(r1));

        customerData = r1[0]; //将查询结果赋给全局变量
        // console.log('customerData:' + JSON.stringify(customerData));

        //黑名单警告
        if (customerData.creditRank === '黑名单') {
            alert('当前客户为黑名单客户!备注:' + customerData.remark + ',备注人:' + customerData.remarker)
        }



        loadContactOptions(customerData)


        let r2 = r1[0]
        delete r2.customerShortName;
        // console.log('r2:' + JSON.stringify(r2));
        fillFormInputs({
            formId: 'rp_borrowbillsForm',
            params: r2
        })
        // $("#rp_borrowbillsForm-customerArea").selectpicker('val', r2.customerArea)
    }

    //选定客户修改事件
    const addCustomerListener = () => {
        $('#rp_borrowbillsForm-customerShortName').unbind();
        $('#rp_borrowbillsForm-customerShortName').change(function () {
            let customerShortName = $('#rp_borrowbillsForm-customerShortName option:selected').val();
            if (customerShortName) {
                $('#rp_borrowbillsForm-customerShortName').selectpicker('refresh');
                fillCustomerInfo(customerShortName);
            }
        });
    }

    //填写产品信息功能
    const fillProductInfo = async (productId) => {
        let r1 = await getDataBySql({
            sql: 'getProduct',
            params: {
                filter: 'productId="' + productId + '"'
            }
        })

        // console.log('r1:' + JSON.stringify(r1));
        let r2 = r1[0]
        delete r2.productId;
        // console.log('r2:' + JSON.stringify(r2));
        fillFormInputs({
            formId: 'rp_borrowsubbillsForm',
            params: r2
        })

        // $('#rp_borrowsubbillsForm-productId').selectpicker('val',r1[0].productId)

    }



    //选定产品修改事件
    const addProductListener = () => {
        // $('#rp_borrowsubbillsForm-productId').unbind();
        $('#rp_borrowsubbillsForm-productId').change(async function () {
            let productId = $('#rp_borrowsubbillsForm-productId option:selected')
                .val()
            if (productId) {
                $('#rp_borrowsubbillsForm-productId').selectpicker('refresh');
                showStockNum()
                fillProductInfo(productId);

                $('#rp_borrowsubbillsForm-num').val(1)

            }
        });

    }

    //仓库选择修改事件
    const addWarehouseListener = () => {
        // $('#rp_outsubbillsForm-warehouseId').unbind();
        $('#rp_borrowsubbillsForm-warehouseId').change(async function () {
            let warehouseId = $('#rp_borrowsubbillsForm-warehouseId option:selected')
                .val()
            if (warehouseId) {
                $('#rp_borrowsubbillsForm-warehouseId').selectpicker('refresh');
                showStockNum();
            }
        });
    }



    //创建借货单新单号
    const createBorrowBillId = async () => {
        let r1 = await getDataBySql({
            sql: "getBorrowBillsNum",
            params: {
                filter: 'borrowBillId like "B-%" and makeDate="' + currentDate() + '"'
            }
        })
        // console.log('r1:' + r1);
        let r2 = r1[0].billsNum
        let r3 = parseInt(r2) + 1;

        let r4 = PrefixInteger(r3, 3);
        // console.log('r4:' + r4);
        let r5 = dateFormat(currentDate(), 'yyyyMMdd');

        let billId = 'B-' + r5 + r4;
        $("#rp_borrowbillsForm-borrowBillId").text(billId);
    }



    //添加主表监听器,定义双击打开事件

    const addMainTableListener = () => {

        //解绑原事件
        $('#rp_borrowbills tbody').unbind();

        //定义主表单击击事件
        $('#rp_borrowbills tbody').on('click', 'tr', async function () {
            //设定选中
            let table = $('#rp_borrowbills').DataTable();

            if ($(this).hasClass('selected')) {
                $(this).removeClass('selected');
                //清空选中单据数据
                mainTableData = {}
                $('#subTablePanel').hide();
            } else {
                table.$('tr.selected').removeClass('selected');
                $(this).addClass('selected');

                let rowSelect = table.row('.selected').data();
                // console.log("rowSelect:" + JSON.stringify(rowSelect));

                mainTableData = rowSelect; //刷新全局主表对象数据

                await loadMainBillSubTable(rowSelect.borrowBillId)

                //打开面板
                $('#subTablePanel').show();

            }

        });


        //定义主表双击事件
        $('#rp_borrowbills tbody').on('dblclick', 'tr', async function () {

            //打开关联信息,打印功能
            $("#accordion").show();

            // $("#listPrintBtn").show();
            // $("#rp_borrowbillsPrint").show();

            //设定选中
            let table = $('#rp_borrowbills').DataTable();
            table.$('tr.selected').removeClass('selected');
            $(this).addClass('selected');
            let dataSelect = table.row('.selected').data();

            mainTableData = dataSelect; //刷新全局主表对象数据

            // console.log('mainTableData', mainTableData);

            //解除客户监听事件
            $('#rp_borrowbillsForm-customerShortName').unbind();


            //重置客户信息改变状态
            customerDataChanged = false
            customerData = {}

            //初始化并填写form内inputs
            initAndFillFormInputs({
                formId: 'rp_borrowbillsForm',
                params: dataSelect
            })

            let r1 = await getDataBySql({
                sql: 'getCustomers',
                params: {
                    filter: 'customerShortName="' + dataSelect.customerShortName + '"'
                }
            })

            // console.log('r1:' + JSON.stringify(r1));

            customerData = r1[0]; //将查询结果赋给全局变量
            //加载备选联系人
            loadContactOptions(customerData);

            //关闭子表panel
            $('#rp_borrowsubbillsPanel').hide();

            //加载子表记录表数据

            loadBillSubTable(dataSelect.borrowBillId);

            //客户选定监听事件
            addCustomerListener();

            //打开面板
            $('#rp_borrowbillsModal').modal('show');

        });

    }

    //主表加载数据函数;

    const loadBillMainTable = async () => {

        //显示过滤模式
        switch (true) {
            case mainTableFilter === 'to_days(makeDate) = to_days(now())':
                $('#btnFilterToday').addClass('active');
                $('#btnFilterUnAudit').removeClass('active');
                $('#btnFilterDiy').removeClass('active');

                break;
            case mainTableFilter === 'status ="待审核"':
                $('#btnFilterToday').removeClass('active');
                $('#btnFilterUnAudit').addClass('active');
                $('#btnFilterDiy').removeClass('active');
                break;
            default:
                $('#btnFilterToday').removeClass('active');
                $('#btnFilterUnAudit').removeClass('active');
                $('#btnFilterDiy').addClass('active');
                break;
        }


        //初始化主表
        loadBillDataTable({
            elementId: 'rp_borrowbills',
            sqlParams: {
                sqlId: 'sqlBorrowBills',
                params: {
                    filter: mainTableFilter,
                    orderBy: 'billSaveTimeStamp desc'
                }
            },
            dtParams: {
                // bAutoWidth: false,//取消自动宽度
                columns: mainColumnsArray,
                // scrollX: true,
                // autoWidth: true
            }
        })
        //清空选项值
        $('.dataTables_filter input').val('')

        //自定义样式调整
        $('.dataTables_filter input').attr({
            'name': 'search',
            'size': 45
        });
        $('.dataTables_filter input').attr({
            'name': 'search',
            'placeholder': '可用表属性过滤'
        });



        // //添加搜索框监听事件,值变动自动隐藏主页附表
        // $('#rp_borrowbills').on('search.dt', function () {
        //     console.log(1111);
        //     $('#subTablePanel').hide();
        // })


        //隐藏搜索值数据列,bStateSave 会导致列中visible 属性失效,采用指定隐藏方案
        let table = $('#rp_borrowbills').DataTable();
        table.column(9).visible(false)

        addMainTableListener();

        //隐藏子面板
        $('#subTablePanel').hide();
    }

    //添加子表监听器,定义双击展开事件
    const addSubTableListener = () => {
        let subTable = $('#rp_borrowsubbills').DataTable();

        //解绑原事件
        $('#rp_borrowsubbills tbody').unbind();

        //定义子表双击事件
        $('#rp_borrowsubbills tbody').on('click', 'tr', function () {

            if ($(this).hasClass('selected')) {
                $(this).removeClass('selected');

                $('#rp_borrowsubbillsPanel').hide();
            } else {
                subTable.$('tr.selected').removeClass('selected');
                $(this).addClass('selected');

                let rowSelect = subTable.row('.selected').data();
                // console.log("rowSelect:" + JSON.stringify(rowSelect));


                //初始化并填写form内inputs
                initAndFillFormInputs({
                    formId: 'rp_borrowsubbillsForm',
                    params: rowSelect
                })

                //禁止修改关键信息
                $('#rp_borrowsubbillsForm-productId').prop("disabled", true);
                $('#rp_borrowsubbillsForm-productId').selectpicker('refresh');
                $('#rp_borrowsubbillsForm-warehouseId').prop("disabled", true);
                $('#rp_borrowsubbillsForm-warehouseId').selectpicker('refresh');
                $('#rp_borrowsubbillsForm-num').prop("disabled", true);






                //打开面板
                $('#rp_borrowsubbillsPanel').show();

                // //选定产品修改事件
                // addProductListener();
            }

        });
    }





    //用数组重载子表
    const loadDataSubTable = async ({
        elementId,
        dataArray
    }) => {

        //加载表格
        await loadBillDataTable({
            elementId: elementId,
            dtParams: {
                dtConfig: 'dtConfigData',
                data: dataArray,
                columns: subColumnsArray,
                // scrollX: true,
                // bAutoWidth: true
            }
        })

        //如数据为0,清空tbody 避免直接获取size =1;
        if (dataArray.length === 0) {
            $('#' + elementId + ' tbody').html("");
        }


        //添加子表双击事件
        addSubTableListener();
    }

    //加载展开选定表子表关联数据

    const loadBillSubTable = async (billId) => {


        loadDataSubTable({
            elementId: 'rp_borrowsubbills',
            dataArray: subTableData
        });

        // loadDataSubTable({
        //     elementId: 'rp_borrowbillsSub',
        //     dataArray: subTableData
        // });

    }

    //加载展开选定表子表关联数据

    const loadMainBillSubTable = async (billId) => {
        let dataArray = await getDataBySql({
            sql: 'sqlBorrowSubBills',
            params: {
                filter: 'borrowBillId="' + billId + '"'
            }
        })
        // console.log('dataArray', dataArray);

        //将表数据同步到共用数组
        subTableData = dataArray;

        loadDataSubTable({
            elementId: 'rp_borrowbillsSub',
            dataArray: subTableData
        });
    }




    //保存借货单函数
    const saveBorrowBill = async () => {


        let mainFormData = getPostFormData({
            formId: 'rp_borrowbillsForm'
        })

        //给主表数据添加,maker 和makeDate日期;新增状态等待单号检测完成,制单人为空则为新增状态

        if (!mainFormData.data[0].maker) {
            await autoUpdateBorrowBillId();
            mainFormData.data[0].maker = sessionName;
            mainFormData.data[0].makeDate = currentDate();

        }

        //更新表单状态
        if (!mainFormData.data[0].status) {
            mainFormData.data[0].status = '待审核';
        }

        // console.log("subTableData:" + JSON.stringify(subTableData))



        // for (let n of subTableData) {
        //     n.borrowBillId = $("#rp_borrowbillsForm-borrowBillId").text();
        //     if (n.status === '新增') {
        //         n.status = '待审核'
        //     }
        // }

        let r1 = await postDBData({
            sql: 'replace',
            params: mainFormData
        })



        for (let n of subTableData) {
            n.status = '预借出'
            delete n.unReturnNum
        }

        let r2 = await postDBData({
            sql: 'replace',
            params: {
                tableId: 'rp_borrowsubbills',
                data: subTableData
            }
        })
        //补回unReturnNum
        for (let n of subTableData) {
            n.unReturnNum = n.num - n.returnNum
        }

        let PIDArr = [];
        for (const n of subTableData) {
            PIDArr.push({
                PID: n.productId,
                warehouseId: n.warehouseId
            })
        }

        let r3 = await initWarehousePID({
            PIDArr: PIDArr
        })

        // console.log('saveBorrowBill r1:' + JSON.stringify(r1));
        // console.log('saveBorrowBill r2:' + JSON.stringify(r2));

        if (r1.affectedRows > 0 && r2.affectedRows > 0 && r3) {
            alert('保存成功!')


        } else {
            alert('出现错误!')
        }

        //更新客户信息功能

        // console.log('customerData:' + JSON.stringify(customerData));
        // console.log('mainFormData:' + JSON.stringify(mainFormData));

        //定义当前表内实际填写客户数据
        let billCustomerData = {
            customerId: mainFormData.data[0].customerId,
            // customerArea: mainFormData.data[0].customerArea,
            customerName: mainFormData.data[0].customerName,
            contact: mainFormData.data[0].contact,
            mobilePhone: mainFormData.data[0].mobilePhone,
            customerBelongShort: mainFormData.data[0].customerBelongShort,
            isInland: mainFormData.data[0].isInland
        }

        //判断当前表内客户数据是否存在更新;

        for (const p in billCustomerData) {
            if (billCustomerData[p] !== customerData[p]) {

                console.log(p + ':' + 'billCustomerData[p]' + billCustomerData[p] + ',customerData[p]:' +
                    customerData[p] + '不同');
                customerData[p] = billCustomerData[p];
                customerDataChanged = true;
                // break;
            }
        }

        console.log('customerData', customerData);

        //存在不同则更新客户信息
        if (customerDataChanged) {

            if (confirm('是否更新该客户基础信息?')) {
                let rc = await postDBData({
                    sql: 'update',
                    params: {
                        tableId: 'rp_customers',
                        data: [customerData],
                        key: 'customerId'
                    }
                })
                if (rc.affectedRows > 0) {
                    alert('更新客户信息成功!')
                }

            }
        }

        // $('#rp_borrowbillsModal').modal('hide');多层modal存在失效问题,改用click关闭
        $('#rp_borrowbillsModalClose').click()

        loadBillMainTable();
        // console.log(mainTableData, subTableData);


    }




    //检查需求单Id函数,防止多人同时填单时出现的覆盖问题

    const checkBorrowBillId = async () => {
        let billId = $("#rp_borrowbillsForm-borrowBillId").text();

        let r1 = await getDataBySql({
            sql: 'getBorrowBillsNum',
            params: {
                filter: 'borrowBillId="' + billId + '"'
            }
        })

        let r2 = r1[0].billsNum;
        // console.log("checkBorrowBillId:" + r2);

        let o = r2 > 0 ? false : true;
        return o;

    }

    //更新需求单号加1
    const updateBorrowBillId = () => {
        let billId = $("#rp_borrowbillsForm-borrowBillId").text();
        if (billId) {
            billIdArr = billId.split('-');
            let newBillId = billIdArr[0] + '-' + (parseInt(billIdArr[1]) + 1);
            $("#rp_borrowbillsForm-borrowBillId").text(newBillId);
        }
    }

    //递归更新单号直到不存在冲突单号;

    const autoUpdateBorrowBillId = async () => {
        let r = await checkBorrowBillId();
        if (r) {
            return true
        } else {
            // console.log('fail:' + $("#rp_borrowbillsForm-borrowBillId").text());
            updateBorrowBillId()
            // console.log('fail11:' + $("#rp_borrowbillsForm-borrowBillId").text());
            return await autoUpdateBorrowBillId();
        }
    }


    //检查删除单据数据
    const checkDeleteBills = async () => {

        let resultData = {
            result: null,
            data: {}
        }

        let table = $('#rp_borrowbills').DataTable();
        let dataSelect = table.rows('.selected').data();
        let mainDBIDArr = [];
        let subDBIDArr = [];
        let billsIdArr = [];

        for (let n = 0; n < dataSelect.length; n++) {
            mainDBIDArr.push(dataSelect[n].DBID);
            billsIdArr.push(dataSelect[n].borrowBillId);
        }

        switch (true) {
            case billsIdArr.length === 0:
                alert('请先选中需要删除的借货单!')

                resultData.result = false;
                return resultData
                break;
            case billsIdArr.length > 0:
                let billIdRange = getRangeString(billsIdArr);
                // console.log("billsIdArr:" + JSON.stringify(billsIdArr));

                let statusArr = await getDataBySql({
                    sql: 'sqlBorrowSubBills',
                    params: {
                        filter: 'borrowBillId in ' + billIdRange
                    }
                })

                console.log("statusArr:" + JSON.stringify(statusArr));
                for (const n of statusArr) {
                    // if (n.status !== '待审核') {
                    //     alert('选中单据已审核,不符合删除条件!')
                    //     resultData.result = false;
                    //     return resultData
                    // }
                    subDBIDArr.push(n.DBID)
                }
                break;
        }

        resultData = {
            result: true,
            data: {
                mainDBIDArr: mainDBIDArr,
                subDBIDArr: subDBIDArr
            }
        }

        return resultData;
    }



    //更新所选产品即时库存
    const showStockNum = async () => {
        let stockNum = 0;
        let productId = $('#rp_borrowsubbillsForm-productId option:selected').val();
        let warehouseId = $('#rp_borrowsubbillsForm-warehouseId option:selected').val();
        // console.log(warehouseId);

        if (productId && warehouseId) {
            stockNum = await getStockNum(productId, warehouseId);
        }
        $('#stockNum').text(stockNum)
    }


    //加载多联系人数据
    const loadContactOptions = (customerData) => {
        // console.log('customerData', customerData);

        //补齐联系人待选项
        let contacts = [];
        if (!customerData.contacts) {
            contacts.push({
                name: customerData.contact,
                phone: customerData.mobilePhone
            })
        }

        if (customerData.contacts) {
            let contactsStr = customerData.contacts;

            // console.log('contactsStr', contactsStr);
            let contactsArr = contactsStr.split(';')
            // console.log('contactsArr', contactsArr);
            for (const n of contactsArr) {
                if (n) {
                    let name = n.split(',')[0];
                    let phone = n.split(',')[1];
                    contacts.push({
                        name: name,
                        phone: phone
                    })
                }
            }
        }

        $("#customerContacts").empty(); //清空原有
        // console.log('contacts', contacts);

        if (contacts.length > 0) {
            for (const contact of contacts) {
                $("#customerContacts").append(
                    "<option data-tokens='" + contact.phone + " ' value='" + contact.name + "'>" + contact
                    .name +
                    "</option>"
                );
            }
            $("#customerContacts").selectpicker('val', customerData.contact);
            $("#customerContacts").selectpicker('refresh');
        }


        //添加监听事件

        $('#customerContacts').unbind();
        $('#customerContacts').change(function () {
            let contact = $('#customerContacts option:selected').val();
            if (contact) {
                $("#customerContacts").selectpicker('refresh'); //刷新显示
                $('#rp_borrowbillsForm-contact').val($('#customerContacts option:selected')
                    .val());
                $('#rp_borrowbillsForm-mobilePhone').val($('#customerContacts option:selected')
                    .data("tokens"));
            }
        });
    }


    function selectFile() {
        $("#file")[0].click();
    }

    //去除excel生成json中的中文属性
    const excelJsonArr2DivJsonArr = (excelJsonArr, rules) => {
        for (let n of excelJsonArr) {
            n = replaceObjParamsName(n, rules)
        }
        return excelJsonArr;
    }

    const addFileReaderListener = () => {
        $("#file")[0].addEventListener('change', function (e) {

            //check-检查文件数量
            var files = e.target.files;
            if (files.length == 0) return;
            var f = files[0];

            //check-检查文件格式
            if (!/\.xlsx$/g.test(f.name)) {
                alert('仅支持读取xlsx格式！');
                return;
            }

            //do-读取数据
            readWorkbookFromLocalFile(f, async function (workbook) {
                let excelData = readWorkbook(workbook);
                let storeData = excelJsonArr2DivJsonArr(excelData, {
                    borrowBillId: '借货单号',
                    customerId: '客户编号',
                    customerShortName: '客户简称',
                    customerName: '客户名称',
                    customerBelongShort: '客户归属',
                    contact: '联系人',
                    mobilePhone: '移动电话',
                    workPhone: '工作电话',
                    operator: '经手人',
                    maker: '制单人',
                    auditor: '审核人',
                    remark: '备注',
                    rowId: '行号',
                    productId: '产品编号',
                    productName: '产品名称',
                    warehouseId: '仓库编号',
                    warehouseName: '仓库名称',
                    unit: '单位',
                    num: '数量',
                    subRemark: '子表备注'
                })

                console.log('storeData', storeData);

                //c-检查导入数据规范
                let billsIdArr = [];
                for (const n of storeData) {
                    if (n.borrowBillId.indexOf('PB-') === -1) { //单据编号以P-开头
                        alert('批量导入借货单号必须以PB-为前缀,请检查')
                        return
                    }

                    billsIdArr.push(n.borrowBillId)
                }

                //c-检查重复单号
                let billIdRange = getRangeString(billsIdArr);
                // console.log("billsIdArr:" + JSON.stringify(billsIdArr));

                let statusArr = await getDataBySql({
                    sql: 'sqlBorrowBills',
                    params: {
                        filter: 'borrowBillId in ' + billIdRange
                    }
                })
                console.log('statusArr', statusArr);

                let dupBillIds = "";
                if (statusArr.length > 0) {
                    for (const n of statusArr) {
                        dupBillIds = dupBillIds + n.borrowBillId + ',';
                    }
                    dupBillIds = dupBillIds.substring(0, dupBillIds.length - 1);
                    alert('以下单号' + dupBillIds + '存在重复单号,请检查!')
                    return
                }

                //do-生成主表,子表数据
                let borrowBillsArr = [];
                let borrowSubBillsArr = [];

                for (const n of storeData) {
                    borrowBillsArr.push({
                        billFrom: '批量导入',
                        borrowBillId: n.borrowBillId,
                        customerId: n.customerId,
                        customerShortName: n.customerShortName,
                        contact: n.contact,
                        mobilePhone: n.mobilePhone,
                        workPhone: n.workPhone,
                        operator: n.operator,
                        maker: n.maker,
                        makeDate: currentDate(),
                        auditor: n.auditor,
                        status: '待审核',
                        remark: n.remark
                    })

                    borrowSubBillsArr.push({
                        borrowBillId: n.borrowBillId,
                        rowId: n.rowId,
                        productId: n.productId,
                        productName: n.productName,
                        warehouseId: n.warehouseId,
                        warehouseName: n.warehouseName,
                        unit: n.unit,
                        num: n.num,
                        status: '预借出',
                        remark: n.subRemark
                    })
                }
                //do-去重处理
                // borrowBillsArr = Array.from(new Set(borrowBillsArr))仅可用于简单数组去重
                var obj = {};
                borrowBillsArr = borrowBillsArr.reduce(function (item, next) {
                    obj[next.borrowBillId] ? '' : obj[next.borrowBillId] = true && item
                        .push(next);
                    return item
                }, [])

                //子表不去重
                // var subObj = {};
                // borrowSubBillsArr = borrowSubBillsArr.reduce(function (item, next) {
                //     subObj[next.borrowBillId] ? '' : subObj[next.borrowBillId] = true && item.push(
                //         next);
                //     return item
                // }, [])
                console.log('borrowBillsArr', borrowBillsArr);
                console.log('borrowSubBillsArr', borrowSubBillsArr);

                //do-导入数据库
                let rInBills = await postDBData({
                    sql: 'insert',
                    params: {
                        tableId: 'rp_borrowbills',
                        data: borrowBillsArr
                    }
                })

                let rInSubBills = await postDBData({
                    sql: 'insert',
                    params: {
                        tableId: 'rp_borrowsubbills',
                        data: borrowSubBillsArr
                    }
                })

                //do-初始化库存
                let PIDArr = [];
                for (const n of borrowSubBillsArr) {
                    PIDArr.push({
                        PID: n.productId,
                        warehouseId: n.warehouseId
                    })
                }

                let newPIDArr = []
                for (let i = 0; i < PIDArr.length; i++) {
                    let isNew = true
                    for (let j = 0; j < newPIDArr.length; j++) {
                        if (PIDArr[i].PID === newPIDArr[j].PID && PIDArr[i].warehouseId ===
                            newPIDArr[j].warehouseId) {
                            isNew = false;
                            break;
                        }
                    }
                    if (isNew) {
                        newPIDArr.push(PIDArr[i])
                    }

                }

                //初始化库存记录
                let rInitPID = await initWarehousePID({
                    PIDArr: newPIDArr
                })

                if (rInBills.affectedRows > 0 && rInSubBills.affectedRows > 0 && rInitPID) {
                    alert('批量导入成功')
                }

                //return-刷新主界面
                loadBillMainTable()


            });
        });
    }


    $(document).ready(function () {

        // alert(parseInt(''))
        //定义全局变量,当前用户名
        sessionName = "<%-locals.session.yjUser.Name%>";
        //定义主表全局变量临时数据对象
        mainTableData = {};
        //定义子表全局变量临时数据数组
        subTableData = [];

        //存储当前客户信息
        customerData = {}
        //记录客户基本信息是否被改变
        customerDataChanged = false
        //主表过滤器
        mainTableFilter = '';


        //过滤器当天按钮
        $('#btnFilterToday').click(function () {
            mainTableFilter = 'to_days(makeDate) = to_days(now())';
            //初始化主表
            loadBillMainTable();

        })


        $('#btnFilterToday').click(); //默认开启当天过滤

        //过滤器待审核
        $('#btnFilterUnAudit').click(function () {
            mainTableFilter = 'status ="待审核"';
            //初始化主表
            loadBillMainTable();

        })


        //过滤器自定义按钮
        $('#btnFilterDiy').click(function () {
            //打开面板
            $('#filterModal').modal('show');
            clearFormInputs({
                formId: 'filterForm'
            })

        })

        //过滤器确认按钮

        $('#confirmFilter').click(function () {
            //获取filterform内数据
            let filter = '';

            //申请日期过滤
            let borrowDateB = $('#filterForm-borrowDateB').val();
            let borrowDateE = $('#filterForm-borrowDateE').val();

            if (borrowDateB) {
                filter = filter + ' and borrowDate>="' + borrowDateB + '"'
            }

            if (borrowDateE) {
                filter = filter + ' and borrowDate<="' + borrowDateE + '"'
            }

            //制单日期过滤
            let makeDateB = $('#filterForm-makeDateB').val();
            let makeDateE = $('#filterForm-makeDateE').val();

            if (makeDateB) {
                filter = filter + ' and makeDate>="' + makeDateB + '"'
            }

            if (makeDateE) {
                filter = filter + ' and makeDate<="' + makeDateE + '"'
            }



            //去除filter中开始的and关键字避免sql错误;

            if (filter) {
                filter = filter.substring(4, filter.length);
            }

            mainTableFilter = filter

            $('#closeFilter').click();
            loadBillMainTable()

        })




        //批量导出按钮
        $('#batchExportBtn').click(async function () {
            $('#exportModal').modal('show')


        })

        //执行批量导出按钮
        $('#doExportBtn').click(async function () {

            let titleTextArr = []
            let titleValueArr = []

            //主表字段选择
            $('#exportModelForm').find('input').each(function () {
                // console.log("this:" + JSON.stringify(this));
                if ($(this).is(':checked')) {

                    let title = $(this).next('label').text();
                    titleTextArr.push(title)
                    let value = $(this).val();
                    titleValueArr.push(value)
                }
            })

            //子表字段选择
            $('#exportSubModelForm').find('input').each(function () {
                // console.log("this:" + JSON.stringify(this));
                if ($(this).is(':checked')) {
                    let title = $(this).next('label').text();
                    titleTextArr.push(title)
                    let value = $(this).val();
                    titleValueArr.push(value)
                }
            })





            exportData = await getDataBySql({
                sql: 'borrowBillsExportSql',
                params: {
                    filter: mainTableFilter
                }
            })
            console.log('exportData', exportData);

            let tableArr = []

            tableArr.push(titleTextArr)

            if (exportData.length > 0) {
                for (let i = 0; i < exportData.length; i++) {
                    let row = []
                    for (const p in exportData[i]) {
                        if (titleValueArr.includes(p)) {
                            row.push(exportData[i][p])
                        }
                    }
                    tableArr.push(row)
                }
            }

            console.log('tableArr', tableArr);



            var sheet = XLSX.utils.aoa_to_sheet(tableArr);
            openDownloadDialog(sheet2blob(
                sheet), '借货单.xlsx');
        })


        //批量导入按钮
        $('#batchImportBtn').click(function () {
            selectFile()
        })

        //数据模板按钮
        $('#importModelBtn').click(function () {
            var aoa = [
                [
                    '单据来源',
                    '借货单号',
                    '客户编号',
                    '客户简称',
                    '客户名称',
                    '客户归属',
                    '联系人',
                    '移动电话',
                    '工作电话',
                    '经手人',
                    '制单人',
                    '审核人',
                    '备注',
                    '行号',
                    '产品编号',
                    '产品名称',
                    '仓库编号',
                    '仓库名称',
                    '单位',
                    '数量',
                    '子表备注'
                ],
            ];
            var sheet = XLSX.utils.aoa_to_sheet(aoa);
            openDownloadDialog(sheet2blob(sheet), '借货单模板.xlsx');
        })



        //新增需求单按钮
        $('#btnBorrowBillNew').click(function () {

            //do-清空form数据
            mainTableData = {}
            //do-清空子表及全局变量
            subTableData = [];


            //do-清空form数据
            clearFormInputs({
                formId: 'rp_borrowbillsForm'
            })

            //do-清空原有选项
            $('#customerContacts').empty();
            $('#customerContacts').selectpicker('destroy');




            //初始化子表
            loadDataSubTable({
                elementId: 'rp_borrowsubbills',
                dataArray: subTableData
            });


            //do-打开面板
            $('#rp_borrowsubbillsPanel').hide();


            //do-清空modal数据
            clearFormInputs({
                formId: 'rp_borrowbillsForm'
            })


            createBorrowBillId();
            $("#rp_borrowbillsForm-borrowDate").val(currentDate());
            $("#rp_borrowbillsForm-isInland").selectpicker('val', '国内')
            $("#rp_borrowbillsForm-sendWay").selectpicker('val', '邮寄过来')

            //在保存时赋值
            // $("#rp_borrowbillsForm-maker").text(sessionName);
            // $("#rp_borrowbillsForm-makeDate").text(currentDate());

            // //客户选定监听事件
            // addCustomerListener();


            //显示/隐藏
            //关闭关联信息,打印按钮
            $("#accordion").hide();

            // $("#listPrintBtn").hide();
            // $("#rp_borrowbillsPrint").hide();

            //打开面板
            $('#rp_borrowbillsModal').modal('show');

        })


        //删除需求单
        $('#btnBorrowBillDelete').click(async function () {
            //检查条件
            let resultData = await checkDeleteBills();
            if (!resultData.result) {
                return;
            }

            //提醒删除风险
            if (!confirm('删除后无法恢复,确认删除?')) {
                return;
            }

            //删除单据

            // let table = $('#rp_borrowbills').DataTable();
            // let dataSelect = table.rows('.selected').data();
            // let DBIDArr = [];
            // for (let n = 0; n < dataSelect.length; n++) {
            //     DBIDArr.push(dataSelect[n].DBID);
            // }

            let r1 = await postDBData({
                sql: 'delete',
                params: {
                    tableId: 'rp_borrowbills',
                    data: resultData.data.mainDBIDArr
                }
            })

            let r2 = await postDBData({
                sql: 'delete',
                params: {
                    tableId: 'rp_borrowsubbills',
                    data: resultData.data.subDBIDArr
                }
            })

            if (r1.affectedRows && r2.affectedRows) {
                alert('删除成功!')
                //重新加载表单
                loadBillMainTable();

            }


        })

        //审核按钮
        $('#btnBorrowBillAudit').click(async function () {
            //check-检查选中
            if (!mainTableData.borrowBillId) {
                alert('请先点击表格选中需要审核的借货单!')
                return
            }
            //check-检查审核条件
            if (mainTableData.status !== '待审核') {
                alert('非待审核状态,请检查!')
                return
            }
            //check-检查审核人
            if (mainTableData.auditor !== sessionName) {
                alert('非指定审核人,请检查!')
                return
            }
            //check-审核确认
            if (!confirm('审核后无法修改,是否继续')) {
                return
            }

            //do-系统出库
            let mainTable = {
                billFrom: '借货单',
                billId: mainTableData.borrowBillId,
                customerId: mainTableData.customerId,
                customerShortName: mainTableData.customerShortName,
                customerName: mainTableData.customerName,
                customerBelongShort: mainTableData.customerBelongShort,
                contact: mainTableData.contact,
                mobilePhone: mainTableData.mobilePhone,
                workPhone: mainTableData.workPhone,
                sendWay: mainTableData.sendWay,
                operator: mainTableData.operator,
                address: mainTableData.address,
                maker: mainTableData.maker,
                makeDate: mainTableData.makeDate,
                auditor: mainTableData.auditor,
                auditDate: currentDate(),
                status: '已审核',
                remark: mainTableData.remark,
            };
            let subTable = [];
            for (const n of subTableData) {
                subTable.push({
                    billId: n.borrowBillId,
                    rowId: n.rowId,
                    productId: n.productId,
                    productName: n.productName,
                    productDescription: n.productDescription,
                    productClass: n.productClass,
                    systemType: n.systemType,
                    warehouseId: n.warehouseId,
                    warehouseName: n.warehouseName,
                    unit: n.unit,
                    num: n.num,
                    status: '已出',
                    remark: n.remark,
                    actType: '借货单',
                    rpBillId: mainTableData.borrowBillId
                })
            }

            let r1 = await autoOutStoreBills({
                mainTable: mainTable,
                subTable: subTable
            })

            // console.log('r1', r1);


            //do-更新借货单状态

            if (!r1) {
                alert('出现错误')
                return;
            }

            let billUpdateData = {
                borrowBillId: mainTableData.borrowBillId,
                status: '已审核'
            }

            let r2 = await postDBData({
                sql: 'update',
                params: {
                    tableId: 'rp_borrowbills',
                    data: [billUpdateData],
                    key: 'borrowBillId'
                }
            })

            //do-更新借货单子表状态
            for (let n of subTableData) {
                if (n.status === '预借出') {
                    n.status = '待归还'
                    n.returnNum = 0
                    delete n.unReturnNum
                }
            }

            let r3 = await postDBData({
                sql: 'replace',
                params: {
                    tableId: 'rp_borrowsubbills',
                    data: subTableData
                }
            })

            //return-操作成功信息
            if (r2.affectedRows > 0 && r3.affectedRows > 0) {
                alert('审核成功,系统已自动生成相应出库单!')
            } else {
                alert('审核失败')
            }

            //do-补齐unReturnNum
            for (let n of subTableData) {
                n.unReturnNum = n.num
            }

            //return-刷新主表
            loadBillMainTable();

        })

        //驳回按钮
        $('#btnBorrowBillReject').click(async function () {
            //check-检查选中
            // console.log(mainTableData);
            if (!mainTableData.borrowBillId) {
                alert('请先点击表格选中需要驳回的借货单!')
                return
            }
            //check-检查驳回条件
            if (mainTableData.status !== '待审核') {
                alert('只能驳回待审核的借货单,请检查!')
                return
            }
            //check-驳回确认
            if (!confirm('驳回后该借货单即将失效,是否继续')) {
                return
            }

            //do-更新借货单状态
            let billUpdateData = {
                borrowBillId: mainTableData.borrowBillId,
                status: '已驳回'
            }

            let r1 = await postDBData({
                sql: 'update',
                params: {
                    tableId: 'rp_borrowbills',
                    data: [billUpdateData],
                    key: 'borrowBillId'
                }
            })

            //do-更新借货单子表状态
            let subbillUpdateData = [];
            for (const n of subTableData) {
                subbillUpdateData.push({
                    borrowBillId: n.borrowBillId,
                    status: '已驳回'
                })
            }

            let r2 = await postDBData({
                sql: 'update',
                params: {
                    tableId: 'rp_borrowsubbills',
                    data: subbillUpdateData,
                    key: 'borrowBillId'
                }
            })

            //return-操作成功信息
            if (r1.affectedRows > 0 && r2.affectedRows > 0) {
                alert('驳回成功!')
            } else {
                alert('驳回失败')
            }
            //return-刷新主表
            loadBillMainTable();

        })







        // 新增填单按钮
        $('#btnAddProduct').click(function () {
            let subTable = $('#rp_borrowsubbills').DataTable();
            //删除子表选中状态
            // subTable.$('tr.selected').removeClass('selected');

            subTable.rows().deselect();

            //清空panel数据
            clearFormInputs({
                formId: 'rp_borrowsubbillsForm'
            })

            //允许修改关键信息

            $('#rp_borrowsubbillsForm-productId').prop("disabled", false);
            $('#rp_borrowsubbillsForm-productId').selectpicker('refresh');
            $('#rp_borrowsubbillsForm-warehouseId').prop("disabled", false);
            $('#rp_borrowsubbillsForm-warehouseId').selectpicker('refresh');
            $('#rp_borrowsubbillsForm-num').prop("disabled", false);






            //打开面板
            $('#rp_borrowsubbillsPanel').show();

            //设置默认值

            $("#rp_borrowsubbillsForm-status").text('新增')
            $("#rp_borrowsubbillsForm-productId").selectpicker('val', '')
            $("#rp_borrowsubbillsForm-warehouseId").selectpicker('val', 'W1')
            $("#rp_borrowsubbillsForm-num").val(1)


        })

        //保存产品按钮

        $('#btnProductSave').click(async function () {
            //check-审核检查
            let auditDate = $('#rp_borrowbillsForm-auditDate').val()
            if (auditDate) {
                alert('已经审核的借货单不允许修改!')
                return
            }



            //库存不足警告
            let stockNum = parseInt($('#stockNum').text());
            let num = parseInt($('#rp_borrowsubbillsForm-num').val())
            if (num > stockNum) {
                // if (!confirm('该产品客服仓当前库存不足,是否依然保存?')) {
                //     return;
                // }
                alert('该产品客服仓当前库存不足,请补充库存后重试!')
                return
            }

            let borrowBillId = $("#rp_borrowbillsForm-borrowBillId").text();

            let rowNum = $("#rp_borrowsubbills tbody tr").size();
            // console.log('rowNum:' + rowNum);
            let productData = getFormData({
                formId: 'rp_borrowsubbillsForm'
            })
            // console.log('productData:' + JSON.stringify(productData.data))
            let rowData = formDataToRowData(productData.data)
            rowData.borrowBillId = borrowBillId;
            rowData.warehouseName = $('#rp_borrowsubbillsForm-warehouseId option:selected').text()
            // console.log('rowData:' + JSON.stringify(rowData))

            rowData.returnNum = 0;
            rowData.unReturnNum = num;

            //检查是否存在序号,区分新增还是编辑
            if (rowData.rowId) {

                for (let n in subTableData) {
                    // console.log("subTableData[n]:" + JSON.stringify(subTableData[n].rowId));
                    // console.log("rowData:" + JSON.stringify(rowData.rowId));

                    //此处不用===避免,字符类型不匹配导致相关问题
                    if (subTableData[n].rowId == rowData.rowId) {

                        subTableData[n] = rowData;
                        break;
                    }
                }
                // console.log("subTableData:" + JSON.stringify(subTableData));

            } else {
                // console.log("subTableData before:" + JSON.stringify(subTableData));
                // console.log("rowData:" + JSON.stringify(rowData.status));
                rowData.rowId = rowNum + 1;



                // let rs = await checkRecordHistory({
                //     productId: rowData.productId
                // })

                // if (rs.length > 0) {
                //     alert('检测到近期存在借货记录,请核实是否为返修品,借货信息如下:\n' +
                //         '制单日期:' + rs[0].makeDate + '\n' +
                //         '借货日期:' + rs[0].finishDate + '\n' +
                //         '借货人:' + rs[0].repairStaff + '\n' +
                //         '故障描述:' + rs[0].faultDescription + '\n' +
                //         '检测结果:' + rs[0].testResult + '\n' +
                //         '更换原件:' + rs[0].changePartList + '\n' +
                //         '费用:' + rs[0].repairLastFee
                //     )
                // }
                // rowData.isRework = '是'
                subTableData.push(rowData);
                // console.log("subTableData:" + JSON.stringify(subTableData));

            }

            //重新加载子表
            loadDataSubTable({
                elementId: 'rp_borrowsubbills',
                dataArray: subTableData
            });

        })

        // 删除产品按钮
        $('#btnDeleteProduct').click(function () {
            let subTable = $('#rp_borrowsubbills').DataTable();
            let rowSelect = subTable.row('.selected').data();
            // alert(JSON.stringify(rowSelect))

            // let table = $('#rp_borrowbills').DataTable();
            // let dataSelect = table.rows('.selected').data();
            // console.log('dataSelect',dataSelect);


            if (rowSelect) {
                if (mainTableData.status !== '待审核') {
                    alert('非待审核状态借货单不允许修改!')
                } else {
                    //删除元素
                    for (const n in subTableData) {
                        if (subTableData[n].rowId === rowSelect.rowId) {
                            subTableData.splice(n, 1);
                        }
                    }
                    //重新排序rowId,避免空缺
                    for (const n in subTableData) {
                        subTableData[n].rowId = parseInt(n) + 1;
                    }

                    //重新加载子表
                    loadDataSubTable({
                        elementId: 'rp_borrowsubbills',
                        dataArray: subTableData
                    });

                    //自动点击新增按钮,防止遗留数据产生错误
                    $('#btnAddProduct').click()

                }

            } else {
                alert('请先点击表格选中需要删除的产品!')
            }
        })


        //添加到可选联系人按钮
        $('#addContactsBtn').click(function () {
            // console.log('customerData', customerData);
            //check-客户id校验
            if (!customerData.customerId) {
                alert('必须先选定客户!')
                return
            }
            //check-信息完整检查
            let contact = $('#rp_borrowbillsForm-contact').val()
            let mobilePhone = $('#rp_borrowbillsForm-mobilePhone').val()
            if (!contact || !mobilePhone) {
                alert('联系人,移动电话未填!')
                return
            }

            //check-是否为已存在联系人
            if (customerData.contacts.indexOf(contact) !== -1 &&
                customerData.contacts.indexOf(mobilePhone) !== -1) {
                alert('该联系人已存在')
                return
            }

            //do-添加到多联系人
            customerData.contacts = customerData.contacts + contact + ',' + mobilePhone + ';'
            customerData.contact = contact
            customerData.mobilePhone = mobilePhone


            //do-加载备选联系人
            loadContactOptions(customerData);

            $("#customerContacts").selectpicker('val', contact);
            $("#customerContacts").selectpicker('refresh');

            customerDataChanged = true


        })



        // //表单产品清单打印按钮
        // $('#listPrintBtn').click(function () {

        //     if (!mainTableData.borrowBillId) {
        //         alert('请先选中需要打印的单据!')
        //         return
        //     }

        //     // console.log('print subTable', subTableData);

        //     let htmlModel = '　<DIV STYLE="page-break-before:always">' +
        //         '<table width="600" border="0">' +
        //         '<tr>' +
        //         '<td width="150"><img src="/images/techmation.png" /></td>' +
        //         '<td width="200" align="center"><h3>宁波弘讯借货码</h3>' +
        //         '<p><h4>repairId</h4></p></td>' +
        //         '<td width="150" align="center"><div id="qrcode" width:100px height:100px /></td>' +
        //         '</tr>' +
        //         '</table>' +
        //         '<table width="600" border="1" class="table table-bordered">' +
        //         '<tr>' +
        //         '<td width="80">客户名称</td>' +
        //         '<td colspan="2">customerShortName</td>' +
        //         '<td width="80">移动电话</td>' +
        //         '<td colspan="2">mobilePhone</td>' +
        //         '</tr>' +
        //         '<tr>' +
        //         '<td>产品编号</td>' +
        //         '<td colspan="2">productId</td>' +
        //         '<td>数量</td>' +
        //         '<td colspan="2">num</td>' +
        //         '</tr>' +
        //         '<tr>' +
        //         '<td>生产日期</td>' +
        //         '<td width="80">productionDate</td>' +
        //         '<td width="80">经手人</td>' +
        //         '<td>operator</td>' +
        //         '<td width="80">客户归属</td>' +
        //         '<td width="80">customerBelongShort</td>' +
        //         '</tr>' +
        //         '<tr>' +
        //         '<td height="50">故障状况</td>' +
        //         '<td colspan="5">faultDescription</td>' +
        //         '</tr>' +
        //         '<tr>' +
        //         '<td>是否加急</td>' +
        //         '<td>urgent</td>' +
        //         '<td>是否返修</td>' +
        //         '<td>isRework</td>' +
        //         '<td>产品归属</td>' +
        //         '<td>productBelong</td>' +
        //         '</tr>' +
        //         '</table>' +
        //         '</DIV>';

        //     //创建模板函数
        //     const fillPrintModel = ({
        //         htmlModel,
        //         repairId,
        //         customerShortName,
        //         mobilePhone,
        //         productId,
        //         num,
        //         productionDate,
        //         operator,
        //         customerBelongShort,
        //         faultDescription,
        //         urgent,
        //         isRework,
        //         productBelong
        //     }) => {
        //         let newHtml = htmlModel
        //             .replace(/repairId/, repairId)
        //             .replace(/qrcode/, 'qrcode-' + repairId)
        //             .replace(/customerShortName/, customerShortName)
        //             .replace(/mobilePhone/, mobilePhone)
        //             .replace(/productId/, productId)
        //             .replace(/num/, num)
        //             .replace(/productionDate/, productionDate)
        //             .replace(/operator/, operator)
        //             .replace(/customerBelongShort/, customerBelongShort)
        //             .replace(/faultDescription/, faultDescription)
        //             .replace(/urgent/, urgent)
        //             .replace(/isRework/, isRework)
        //             .replace(/productBelong/, productBelong)

        //         return newHtml;
        //     }
        //     //创建二维码函数
        //     const createQrcode = ({
        //         repairId
        //     }) => {
        //         var qrcode = new QRCode(document.getElementById("qrcode" + '-' + repairId), {
        //             width: 100,
        //             height: 100
        //         });
        //         qrcode.makeCode(repairId);
        //     }

        //     //创建信息表函数
        //     const createRpHtml = (subTableData) => {
        //         if (subTableData.length > 0) {
        //             let divHtml = ''
        //             for (let i = 0; i < subTableData.length; i++) {
        //                 const element = subTableData[i];
        //                 let newHtml = fillPrintModel({
        //                     htmlModel: htmlModel,
        //                     repairId: subTableData[i].borrowBillId + '-' + subTableData[i]
        //                         .rowId,
        //                     customerShortName: mainTableData.customerShortName,
        //                     mobilePhone: mainTableData.mobilePhone,
        //                     productId: subTableData[i].productId,
        //                     num: subTableData[i].num,
        //                     productionDate: subTableData[i].productionDate,
        //                     operator: mainTableData.operator,
        //                     customerBelongShort: mainTableData.customerBelongShort,
        //                     faultDescription: subTableData[i].faultDescription,
        //                     urgent: subTableData[i].urgent,
        //                     isRework: subTableData[i].isRework,
        //                     productBelong: subTableData[i].productBelong

        //                 })
        //                 //删除第一页换页
        //                 if (i === 0) {
        //                     newHtml = newHtml.replace(/STYLE="page-break-before:always"/, '');
        //                 }
        //                 divHtml = divHtml + newHtml
        //             }
        //             $('#listPrintViewDiv').html(divHtml)
        //         }
        //     }


        //     createRpHtml(subTableData);

        //     //生成借货二维码
        //     const createQrcodes = (subTableData) => {
        //         if (subTableData.length > 0) {
        //             for (const n of subTableData) {
        //                 createQrcode({
        //                     repairId: n.borrowBillId + '-' + n.rowId
        //                 })
        //             }
        //         }
        //     }


        //     createQrcodes(subTableData);








        //     // fillPrintModel()

        //     // let formData = getFormData({
        //     //     formId: 'rp_borrowbillsForm',
        //     // })

        //     // fillTableInputs({
        //     //     tableId: 'printViewMainTable',
        //     //     params: formDataToRowData(formData.data)
        //     // })

        //     // fillTableRows({
        //     //     tableId: 'printViewSubTable',
        //     //     cols: ['rowId', 'productId', 'productName', 'num'],
        //     //     data: subTableData
        //     // })

        //     $('#listPrintViewModal').modal('show')
        // })

        // //确认打印按钮
        // $('#listPrintConfirmBtn').click(function () {
        //     Print('#listPrintViewDiv', {
        //         onStart: function () {
        //             console.log('onStart', new Date())
        //         },
        //         onEnd: function () {
        //             console.log('onEnd', new Date())
        //         }
        //     })
        // })


        // //打印关闭按钮
        // $('#listPrintCloseBtn').click(function () {
        //     $('#listPrintViewModal').modal('hide')
        // })

        //表单打印按钮
        $('#rp_borrowbillsPrint').click(function () {

            // let formData = getFormData({
            //     formId: 'rp_borrowbillsForm',
            // })

            // fillTableInputs({
            //     tableId: 'printViewMainTable',
            //     params: formDataToRowData(formData.data)
            // })
            // console.log('print mainTableData:', mainTableData);
            if (!mainTableData.borrowBillId) {
                alert('请先选中需要打印的单据!')
                return
            }


            fillTableInputs({
                tableId: 'printViewMainTable',
                params: mainTableData
            })


            fillTableRows({
                tableId: 'printViewSubTable',
                cols: ['rowId', 'productId', 'productName', 'unit', 'num'],
                data: subTableData
            })

            $('#printViewModal').modal('show')
        })

        //确认打印按钮
        $('#confirmPrintBtn').click(function () {
            Print('#printViewDiv', {
                onStart: function () {
                    console.log('onStart', new Date())
                },
                onEnd: function () {
                    console.log('onEnd', new Date())
                }
            })
        })


        //打印关闭按钮
        $('#printCloseBtn').click(function () {
            $('#printViewModal').modal('hide')
        })

        //表单保存按钮
        $('#rp_borrowbillsSave').click(function () {

            //check-单据状态
            let billStatus = $("#rp_borrowbillsForm-status").text()
            if (billStatus !== '' && billStatus !== '待审核') {
                alert('只能保存待审核单据,请检查!')
                return;
            }

            //check-保存条件
            if (mainTableData.status === '已审核') {
                alert('已审核单据无法保存,请检查')
                return
            }

            //check-检查必填项
            if (!$('#rp_borrowbillsForm-customerId').text()) {
                alert('未选定客户,请检查!')
                return;
            }

            //不存在子表项目则中断保存
            if (subTableData.length === 0) {
                alert('未添加需要借货的产品,无法保存单据,请检查!')
                return;
            }

            //c-顺付备注检查
            if ($('#rp_borrowbillsForm-expressType option:selected').text() === '顺付' &&
                !$("#rp_borrowbillsForm-remark").val()) {
                alert('顺付必须在备注中写明原因!')
                return
            }

            saveBorrowBill();
        })

        //初始化过滤器日期组
        loadDatePickers({
            elementIds: [
                'filterForm-borrowDateB',
                'filterForm-borrowDateE',
                'filterForm-makeDateB',
                'filterForm-makeDateE'
            ]
        })


        //初始化组件
        loadDatePicker({
            elementId: "rp_borrowbillsForm-borrowDate",
        })


        loadDatePicker({
            elementId: "rp_borrowsubbillsForm-productDate",

        })



        //初始化selector

        loadBootStrapSelector({
            elementId: 'rp_borrowbillsForm-customerShortName',
            sqlParams: {
                sql: 'getCustomerSelector'
            },
        })




        loadBootStrapSelector({
            elementId: 'rp_borrowbillsForm-operator',
            sqlParams: {
                sql: 'getStaffSelector'
            },
            // initValue:'中国,北京'
        })


        loadBootStrapSelector({
            elementId: 'rp_borrowsubbillsForm-productId',
            sqlParams: {
                sql: 'getProductSelector',
                params:{
                    filter:'partBelong in ("零件","面板","主机","其他","液晶","选配")'
                }
            },
        })



        loadBootStrapSelector({
            elementId: 'rp_borrowbillsForm-isInland',
            sqlParams: {
                sql: 'getOptionSelector',
                params: {
                    filter: 'optionClass="国内外"',
                    orderBy: 'orderId'
                }
            },
        })

        loadBootStrapSelector({
            elementId: 'rp_borrowbillsForm-customerBelongShort',
            // initValue:'',
            sqlParams: {
                sql: 'getCustomerSelector',
                params: {
                    filter: 'customerType="客户"'
                }
            },
            // initValue:'中国,北京'
        })


        loadBootStrapSelector({
            elementId: 'rp_borrowbillsForm-sendWay',
            sqlParams: {
                sql: 'getOptionSelector',
                params: {
                    filter: 'optionClass="出货方式"',
                    orderBy: 'orderId'
                }
            },
        })

        loadBootStrapSelector({
            elementId: 'rp_borrowbillsForm-expressType',
            sqlParams: {
                sql: 'getOptionSelector',
                params: {
                    filter: 'optionClass="快递类型"',
                    orderBy: 'orderId'
                }
            },
        })





        loadBootStrapSelector({
            elementId: 'rp_borrowbillsForm-auditor',
            sqlParams: {
                sql: 'getStaffSelector'
            },
            // initValue:''
        })

        loadBootStrapSelector({
            elementId: 'rp_borrowsubbillsForm-warehouseId',
            sqlParams: {
                sql: 'getWarehouseSelector',
            },
            // initValue: 'W1'
        })






        //客户选定监听事件
        addCustomerListener();

        //选定产品修改事件
        addProductListener();

        //仓库选择修改事件
        addWarehouseListener();

        //添加文件监听事件
        addFileReaderListener()



        // 关闭事件重置选中datatable选中,避免显示错误
        $('#rp_borrowbillsModal').on('hidden.bs.modal', function () {
            let table = $('#rp_borrowbills').DataTable();
            table.$('tr.selected').removeClass('selected');
            mainTableData = {}

            // //添加监听出来扫描modal关闭 表单modal失效问题
            $(this).removeData('bs.modal');
            $(document.body).addClass("modal-open");
        })

        //自动调整modal显示位置
        $('#scanModal').on('show.bs.modal', function (
            e) {
            // 关键代码，如没将modal设置为 block，则$modala_dialog.height() 为零 
            $(this).css('display', 'block');
            // var modalHeight = $(window).height() / 2;
            let modalHeight = $(window).height() / 3;
            $(this).find('.modal-dialog').css({
                'margin-top': modalHeight
            });
        });






    });
</script>